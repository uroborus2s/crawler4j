# External Modules Only

This directory is intentionally kept as an empty placeholder.

Builtin business modules have been removed from the repository.
Crawler4j now loads real modules from externally installed packages under the app data directory,
or from explicit development links when debugging module source trees.
