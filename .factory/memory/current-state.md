# 当前状态

- 当前模式：Default
- 当前阶段：IMPLEMENTATION
- 活跃任务：1
- 活跃变更：1
- 活跃缺陷：0
- 活跃 PR：1

- 角色目录总数：9
- 当前阶段主要角色：项目协调者、后端工程师、前端工程师、测试工程师、文档与记忆管理员

- 当前技术画像：Crawler4j Model 项目画像
- 技术画像预设：crawler4j-model
- 关键工程规则数：5
- 设计交付物数：1

## 最近条目

- 最新修正：2026-06-18 根应用 / 运行时版本已提升到 `0.4.16`，用于修复来源代理同步匹配规则；SDK / Contracts 继续保持 `0.4.2`。来源代理回查仍优先采用外部指纹浏览器返回的结构化真实 `host/port`，但绑定 IP 表时只要求 `host + port` 唯一命中，不再比较协议、用户名或密码，解决来源为 `SOCKS5`、IP 表记录为 `http` 时无法绑定的问题。验证：REM 来源代理同步 / 环境列表 / 版本服务回归 `54 passed`，目标 `ruff check`、`uv lock --check`、`.factory/project.json` JSON 校验与 `git diff --check` 通过。
- 最新修正：2026-06-16 根应用 / 运行时版本已提升到 `0.4.15`，用于修复 VirtualBrowser 来源代理解析优先级并收紧绑定语义；SDK / Contracts 继续保持 `0.4.2`。当 VirtualBrowser API 同时返回结构化 `proxy.host/port/protocol/user/pass` 与 `proxy.url=http://127.0.0.1:本地端口` 时，来源代理同步现在优先采用结构化真实代理；只有唯一命中 IP 表条目时才写入 `pool_id/ip_entry_id` 绑定，未命中 IP 表时跳过或清除本地错误绑定，不再把非 IP 表代理保存成绑定 IP。验证：REM 来源代理同步 / 环境列表 / 版本服务回归 `53 passed`，目标 `ruff check`、`uv lock --check`、`.factory/project.json` JSON 校验与 `git diff --check` 通过。
- 最新修正：2026-06-16 根应用 / 运行时版本已提升到 `0.4.14`，用于已导入指纹浏览器环境的来源代理同步与 IP 表唯一匹配绑定；SDK / Contracts 继续保持 `0.4.2`。`ProviderEnvInfo` 现在保留来源代理配置，VirtualBrowser 未同步环境列表会解析来源 `proxy` 并构造 `ProxyConfig`；新导入环境自动把来源代理保存到本地环境记录，并在 IP 表中按协议、host、port、用户名、密码唯一匹配时写入 `pool_id/ip_entry_id`；环境管理页新增“同步来源代理”按钮，先预览历史已导入环境的同步计划，再批量写回本地代理绑定。验证：REM 来源代理同步 / 环境列表 / 版本服务回归 `49 passed`，目标 `ruff check`、`uv lock --check`、`.factory/project.json` JSON 校验与 `git diff --check` 通过。
- 最新修正：2026-06-15 根应用 / 运行时版本保持 `0.4.13`，并补充环境管理列表展示环境绑定的代理 IP；SDK / Contracts 继续保持 `0.4.2`。运行环境表格新增“绑定 IP”列，优先从 `Environment.proxy_config.static_value/current_ip/ip_entry_id` 生成 `host:port` 展示，tooltip 展示代理模式、协议、用户名、IP 条目和 IP 池，密码不在列表中展示；旧环境只有 `current_ip` 时也能展示。验证：REM 环境列表 + 版本服务回归 `27 passed`，目标 `ruff check`、`uv lock --check`、`.factory/project.json` JSON 校验与 `git diff --check` 通过。
- 最新修正：2026-06-15 已将三包版本事实源提升到 `crawler4j 0.4.13`、`crawler4j-sdk 0.4.2`、`crawler4j-contracts 0.4.2`。SDK / Contracts 0.4.2 用于对外发布已有环境导入 workflow 场景契约、SDK CLI `--host-scenario existing_env_import` 脚手架和扫描元数据；根应用 0.4.13 承接 `env.get_proxy` 当前环境代理读取与多环境导入批次元数据。`packages/*/pyproject.toml`、`uv.lock`、README、发布文档和 memory 已同步，SDK / Contracts wheel/sdist 已构建并按 `contracts -> sdk` 顺序上传 PyPI；版本/打包配置回归 `65 passed`，`uv lock --check`、`.factory/project.json` JSON 校验与 `git diff --check` 通过。
- 最新重构：2026-06-15 已按 0.4.0 分支方案开始收口“已有环境导入 + 环境代理读取”契约。`@workflow` 新增 `host_scenarios=["existing_env_import"]`，Contracts、SDK scanner、Core MMS descriptor 和 SDK CLI 脚手架已贯通；宿主导入对话框和导入服务只允许选择声明该场景的 workflow，避免误选普通 workflow。多环境导入会为同一批次生成 `import_group_id` 并写入 `ctx.runtime["creation_params"]` 与导入状态元数据。`ProxyConfig` 新增 `ip_entry_id`；新 IP 池绑定会直接持久化条目 ID，旧环境在模块调用 `ctx.tools.call("env.get_proxy")` 时按池和地址信息懒回填。开发者指南、设计契约和聚焦单测已同步。
- 最新修正：2026-06-13 根应用 / 运行时版本已单独提升到 `0.4.12`，用于承接 Hosted UI DataTable 行按钮显式 params 分发和任务暂停后绑定业务行 `run_status` 释放修复；SDK / Contracts 继续保持 `0.4.1`，不随本次根应用升版。当前仍需补 `0.4.12` Windows/macOS 客户端包、正式 tag / release 和真机升级证据。
- 最新修复：2026-06-13 已修复“释放”按钮仍无法触发和暂停任务后账号仍显示 `占用中` 的回归。`ManagedPageRenderer` 现在从当前行的 action spec 中查找匹配 action id，优先使用 action spec 的 `name` 调用同名 `@ui_action`，并按显式 `params` binding/value 从当前行组装参数，例如 `{phone: row.phone}`；未声明 params 时才退回 `crud.primary_key`。`ExecutionRunner` 在任务终态释放环境 lease 后会按模块 `env_binding_field` 扫描可写 `run_status` 的业务表，把当前 env id 且仍为 `占用中` 的行兜底更新为 `不占用`，避免暂停后业务账号占用态残留。
- 最新修正：2026-06-13 根应用 / 运行时版本已单独提升到 `0.4.11`，用于承接 Hosted UI DataTable 自定义行按钮分发到同名 `@ui_action` 的客户端修复；SDK / Contracts 继续保持 `0.4.1`，不随本次根应用升版。当前仍需补 `0.4.11` Windows/macOS 客户端包、正式 tag / release 和真机升级证据。
- 最新修复：2026-06-13 已修复 Hosted UI DataTable `actions` 列自定义行按钮未分发到模块 `@ui_action` 的问题。`ManagedPageRenderer` 现在统一接收 `SkyDataTable.row_action_requested`，`__crud_update__` / `__crud_delete__` 继续走内置编辑和删除流程，其他 action id 调用同名 `@ui_action`，并用 `crud.primary_key` 从当前行组装单个命名参数，例如 `{phone: row.phone}`；新增回归覆盖自定义行按钮与 CRUD 行动作共存时的分发行为。
- 最新修正：2026-06-13 根应用 / 运行时版本已单独提升到 `0.4.10`，用于承接任务监控暂停后对象 cleanup 链路 `asyncio.CancelledError` 截断修复；SDK / Contracts 继续保持 `0.4.1`，不随本次根应用升版。当前仍需补 `0.4.10` Windows/macOS 客户端包、正式 tag / release 和真机升级证据。
- 最新修复：2026-06-13 已修复任务监控暂停后 `core-native-v2` 对象 cleanup 链可能被 `asyncio.CancelledError` 截断的问题。暂停按钮仍走 `TaskService.pause_job()` -> `JobController.request_job_stop()` -> `TaskDispatcher.request_stop_for_job()` -> `TaskContext.request_stop()`，ATM 会 cancel 正在运行的模块协程并等待 MMS `finally` 中的 cleanup 完成后再回收环境；本次补齐 `ObjectContainerV2._cleanup_instance()` 在用户停止或超时收尾场景下把单个对象 cleanup 抛出的 `CancelledError` 记录为该对象清理失败，并继续清理后续 component/workflow，避免前序组件清理被停止信号打断后阻止账号运行态和审计写回。新增回归覆盖 stop cleanup 抛 `CancelledError` 后继续清理后续对象，以及 ExecutionRunner 在释放环境前等待模块取消收尾完成。
- 最新修正：2026-06-12 根应用 / 运行时版本已单独提升到 `0.4.9`，用于承接运行模板指定环境选择、Core Hosted UI DataTable 可见筛选排序、IP 池条目人工可用状态等客户端改动；SDK / Contracts 继续保持 `0.4.1`，不随本次根应用升版。当前仍需补 `0.4.9` Windows/macOS 客户端包、正式 tag / release 和真机升级证据。
- 最新开发：2026-06-12 ATM 运行模板选择已有环境阶段 1 已落地。UI 新增 `指定环境 / 候选函数` 切换，默认保存固定 `env_id`；固定环境下拉只列当前模块可用的 `READY + BROWSER + 无租约` 环境，且环境未归属或已归属当前模块，不列出其他模块环境、非浏览器环境、非就绪环境或已租约环境。候选函数保留给账号状态、黑号、等级等业务动态筛选和 Service Job 等待队列。作业预览、任务详情与运行摘要已同步显示“指定环境”或“候选查询”。
- 最新修复：2026-06-12 已补齐 Core Hosted UI DataTable 的可见筛选与排序入口。`SkyDataTable` 现在保留 select 列 `options`，对 `type="select"`、`searchable=True` 的列渲染工具栏快速筛选下拉，并把非全量选项写入 `HostedDataTableQuery.params`；启用排序且存在 `sortable=True` 列时，工具栏会显示排序字段与升/降序控件，并与原表头点击排序状态同步。`ManagedPageRenderer._normalize_table_query_for_handler()` 改为合并页面导航参数与表格 params，表格显式筛选同名优先，避免携游账号管理页 `record_status` 筛选在调用 query handler 前丢失。开发者指南、设计契约和压缩运行卡已同步。
- 最新修复：2026-06-12 IP 池条目新增人工 `可用 / 不可用` 状态。`ip_entries.status` 启动时对旧库补列并默认 `available`；`IPPool.select_ip()` 在所有分配策略前统一过滤不可用条目；`IPPoolManager.set_entry_status()` 只更新条目状态，不解绑已有环境；`bind_ip()` 现在先确认有可用候选，选不到时保留当前环境旧绑定和绑定数。IP 池页面新增状态列和 `停用 / 启用` 行操作。验证：IP 池聚焦回归 `23 passed`，REM 目录回归 `153 passed`，状态库迁移 + runtime capabilities 回归 `41 passed`，目标 `ruff check` 与 `git diff --check` 通过。
- 最新修正：2026-06-11 根应用 / 运行时版本已单独提升到 `0.4.8`，用于承接 IP 池最久未使用默认分配策略、最近使用时间记录与旧库迁移的客户端修复版；SDK / Contracts 继续保持 `0.4.1`，不随本次根应用升版。验证：`uv lock --check` 通过；版本服务 + REM IP 池 / 运行模板定向回归 `53 passed`；目标 `ruff check`、`.factory/project.json` JSON 校验与 `git diff --check` 通过。当前仍需补 `0.4.8` Windows/macOS 客户端包、正式 tag / release 和真机升级证据。
- 最新修复：2026-06-11 IP 池默认分配策略已从“最少绑定”切到“最久未使用”。新增 `IPStrategy.LEAST_RECENTLY_USED`，新建默认池、IP 池新建弹窗和运行模板绑定策略默认都使用 `least_recently_used`；旧 `least_bound` 继续保留为显式可选策略。`ip_entries` 新增 `updated_at` 与 `last_used_at`，旧库启动时自动补列并创建 `(pool_id, last_used_at)` 索引；绑定成功后记录最近使用时间，IP 池页面展示“最近使用”。定向回归 `50 passed`，扩展回归 REM `147 passed`、persistence `82 passed`。
- 最新修正：2026-06-09 根应用 / 运行时版本已单独提升到 `0.4.7`，用于承接对象 cleanup 固定超时移除后的客户端修复版；SDK / Contracts 继续保持 `0.4.1`，不随本次根应用升版。验证：`uv lock --check` 通过；版本服务 + MMS 对象 cleanup 生命周期定向回归 `58 passed`；目标 `ruff check`、`.factory/project.json` JSON 校验与 `git diff --check` 通过。当前仍需补 `0.4.7` Windows/macOS 客户端包、正式 tag / release 和真机升级证据。
- 最新修复：2026-06-09 已移除 `core-native-v2` workflow/component 对象生命周期 `cleanup(ctx, outcome)` 的宿主固定执行超时。Core 仍会在成功、失败、异常、主体超时、用户停止或 setup 失败后按 component 依赖反向顺序再到 workflow 调用 cleanup，但不再传入 30 秒预算，也不再在 `ObjectContainerV2.cleanup()` 内对对象 cleanup 包 `asyncio.wait_for`，避免业务收尾被短超时截断；cleanup 异常仍只记录日志，不覆盖任务终态，环境回收超时仍由 `atm.env_recycle_timeout_seconds` 单独控制。新增回归锁定 `ModuleService._run_v2_workflow()` 不再限制对象 cleanup 超时。
- 最新修正：2026-06-07 已将根应用 / 运行时版本事实源从 `0.4.5` 修正提升到 `0.4.6`，用于承接指纹浏览器生命周期串行化修复版；SDK / Contracts 继续保持 `0.4.1`，不随本次根应用升版。验证：`uv run pytest packages/crawler4j/tests/unit/test_core/test_system/test_version_service.py packages/crawler4j/tests/unit/test_core/test_rem/test_provider.py packages/crawler4j/tests/unit/test_core/test_rem/test_bitbrowser_provider.py packages/crawler4j/tests/unit/test_core/test_rem/test_virtualbrowser_client.py packages/crawler4j/tests/unit/test_core/test_rem/test_destroy_env.py packages/crawler4j/tests/unit/test_core/test_rem/test_post_create_actions.py -q` => `56 passed`；目标 `ruff check` 与 `uv lock --check` 通过。当前仍需补 `0.4.6` Windows/macOS 客户端包、正式 tag / release 和真机升级证据。
- 最新修复：2026-06-06 已针对 VirtualBrowser 多并发启动导致客户端假死的问题收口宿主侧启动链。`VirtualBrowserProvider` 现在对创建环境 `addBrowser`、打开窗口 `launchBrowser` 和 Playwright/CDP `connect` 使用同一生命周期锁串行执行，避免 3 个以上任务同时冲击 VirtualBrowser 本地管理 API 与 CDP 连接阶段；新增并发 `open()` 回归锁定 `launchBrowser` 不会并发进入。定向验证：`uv run pytest packages/crawler4j/tests/unit/test_core/test_rem/test_provider.py -q` => `10 passed`；`uv run pytest packages/crawler4j/tests/unit/test_core/test_rem/test_provider.py packages/crawler4j/tests/unit/test_core/test_rem/test_virtualbrowser_client.py -q` => `25 passed`；目标文件 `uv run ruff check ...` 通过。尚未完成 Windows 真机客户端 smoke 与真实 VirtualBrowser 多并发留证。
- 发布计划：2026-06-13 根应用 / 运行时版本已单独提升到 `0.4.12`，用于本轮 Hosted UI DataTable 行按钮显式参数分发和暂停后绑定占用态释放修复；SDK / Contracts 继续保持 `0.4.1`，不随本次根应用升版。当前需通过 PR 合并到 `main`，Windows/macOS 更新包需在构建机补齐。
- 最新修复：2026-05-30 已修复开发模块源码目录扫描对 `.venv/` symlink 的误报。Core manifest lock 校验和 SDK 打包文件收集现在先跳过 `.venv/`、`dist/`、`build/`、`.git/`、缓存目录与 `*.egg-info/`，再对真实模块文件执行 symlink 拒绝和路径越界检查；DevLink/源码预检不再因为 `.venv/bin/python3` 报“模块文件不能是符号链接”，非忽略目录 symlink 与 ZIP 内 symlink/路径穿越仍保持拒绝。定向回归覆盖源码预检与 SDK `_archive_members()`。
- 最新修复：2026-05-27 VirtualBrowser 启动就绪与 `addBrowser` 诊断已增强。ExternalApp 对 VirtualBrowser 不再只用根路径端口探测判定 API 就绪，而是调用 `/api/getBrowserList` 并要求返回 `success=true`；VirtualBrowser 本地管理 API 请求统一使用 `127.0.0.1` 且不读取系统代理；`addBrowser` 对启动期 `Relay failed` 5xx 做短重试，并在失败日志中输出 endpoint、attempt、响应正文和脱敏 payload。根应用版本事实源提升到 `crawler4j 0.4.4`，SDK / Contracts 继续保持 `0.4.1`。
- 最新修复：2026-05-26 REM 环境列表刷新误删环境已修复。刷新按钮现在只从数据库重载环境池并刷新列表，不再执行 `run_gc`，避免 VirtualBrowser `exists()` 外部判定不稳时误删 READY 环境；“清理环境”和显式销毁仍保留删除入口。根应用版本事实源提升到 `crawler4j 0.4.3`，SDK / Contracts 继续保持 `0.4.1`。
- 发布计划：2026-05-27 根应用 / 运行时版本已单独提升到 `0.4.4`，用于 VirtualBrowser 启动就绪竞态和 `addBrowser` relay 500 诊断修复版；SDK / Contracts 继续保持 `0.4.1`，不随本次根应用升版。当前需推远端、创建 PR 合并到 `main`，Windows 更新包需在 Windows 构建机补齐。
- 最新修复：2026-05-18 Windows 客户端点击 `关于 -> 检查更新/升级` 后长时间“未响应”的问题已定位为 UI 主线程同步执行 Velopack `check_for_updates/download_updates/apply_updates_and_restart`。当前已改为后台 `QThread` 执行宿主自更新流程，执行期间按钮禁用并显示下载提示，完成后恢复按钮和结果文案；新增回归锁定更新流程不得在 UI 线程运行。
- 历史发布计划：2026-05-18 发布候选曾从 `0.4.0` 提升到 `0.4.1`，用于绕开 PyPI 0.4.0 删除文件名不可复用阻塞；SDK / Contracts 已按 `0.4.1` 发布到 PyPI，macOS 0.4.1 客户端升级包已生成上传。根应用后续曾单独提升到 `0.4.2`，当前已进一步提升到 `0.4.3`。
- 发布阻塞：2026-05-18 已重建 `crawler4j-contracts 0.4.0` / `crawler4j-sdk 0.4.0` wheel 与 sdist，并尝试按依赖顺序执行 `uv run publish crawler4j-contracts`。PyPI 返回 `400 This filename was previously used by a file that has since been deleted`，说明 `crawler4j_contracts-0.4.0-py3-none-any.whl` 曾上传后删除，PyPI 不允许复用同一文件名；因此 `crawler4j-contracts 0.4.0` 不能重发，`crawler4j-sdk 0.4.0` 暂不发布以避免依赖 `crawler4j-contracts>=0.4.0,<0.5.0` 无法解析。后续外部发布需改发新版本号或通过 PyPI 项目管理侧处理历史删除记录。
- 最新修复：2026-05-18 已排查 `ctx.db.into(...).delete_where(...)` 删除慢的问题。delete 路径最终是单条 SQL 并由 `DbWriteCoordinator` 包装短事务；本轮发现 managed_dataset 按主键快捷删除会落到物理 `record_key`，但 `module_datasets` 原先只有 `(module_name)` 和 `(module_name, dataset_name)` 索引，缺少 `(module_name, dataset_name, record_key)` 复合索引。现已补齐 `idx_module_datasets_record_key`，旧库启动时也会通过 `CREATE INDEX IF NOT EXISTS` 补上；开发者指南同步建议高频主键删除使用 `delete_where(where=<主键值>)`。
- 最新修复：2026-05-12 `HostedDataTableQuery` 表格查询 helper 已重构为 `to_query(...)` / `to_result(...)`。旧 rows/count 双 callback 方法已删除；新 API 接收 rows/count 查询 Callable，并通过单字段 `search_transform/sort_transform/filter_transform` 在内部循环转换字段，返回 `None` 时过滤字段。`sort_transform` 只允许转换排序字段名，方向值保留原 `HostedDataTableSortSpec.direction`；`filter_transform` 可返回字段名或 `(new_key, new_value)`。`HostedDataTableQueryResult` 新增 `from_query(query, query_result, transform)`，按 `query.page/page_size` 自动生成标准结果。
- 最新修复：2026-05-12 已收紧 Hosted UI DataTable CRUD handler 契约。SDK 扫描期现在校验 `create_handler/update_handler/delete_handler` 必须引用已扫描的 `@ui_action`，并分别使用确定签名 `(context, payload)`、`(context, <primary_key>, payload)`、`(context, <primary_key>)`；`payload` 不能再用 `Mapping[str, Any]` 或裸 `dict` 模糊接收，而应使用模块自定义 `TypedDict` / dataclass 风格输入类型，主键参数也必须标注具体标量类型。Contracts 已导出 CRUD payload/result 类型入口，SDK 脚手架和 0.4.0 开发者文档已同步。
- 最新文档：2026-05-12 已按 0.4.0 当前代码和模块开发者/使用者双审阅意见重整 `docs/03-developer-guide/v0.4.0/`。开发者指南现在明确 `module init` 默认骨架与新增业务能力的命名边界，统一 `check structure -> manifest lock -> check full`、`package build` 前置 lock、DevLink/ZIP 安装的宿主 shell 与模块 shell 边界，并把 Hosted UI、`@ui_action`、manifest lock 当前结构、候选环境与清理 DSL 等 0.4.0 行为融入现有文档框架。
- 最新审查修复：2026-05-12 上线前专项 code review 已完成 4 组审查并进入修复闭环。已修复 SDK 扫描期 `DataTable.query_handler` 漏检，固定环境 claim 终态刷新，`custom_table` bool 字符串写入解析，Hosted UI qasync 阻塞弹窗路径，MMS UI bridge 旧 `call_page_action*` 残留，`managed_resource` 前 1000 行截断，以及桌面帮助页开发者指南版本路径漂移。文档与测试记忆已同步；QA 定向验证均通过，当前分支全量 `PYTHONDONTWRITEBYTECODE=1 uv run pytest -q -p no:cacheprovider` => `983 passed`，`uv run ruff check .` 与 `git diff --check` 通过。生产发布 Gate 仍不能直接放行：`ctrip` 真实站点 DevLink + ZIP E2E、Windows 真机签名/安装/自更新、SDK/Contracts 外部发布证据仍需独立闭环。
- 最新修复：2026-05-11 已把 Hosted UI CRUD 从 `page_action` 旧路径收口到 `@ui_action` 命名参数模型。`DataTable.crud.create_handler` 调用 `handler(ctx, payload=...)`，`update_handler` 调用 `handler(ctx, <primary_key>=row_key, payload=...)`，`delete_handler` 调用 `handler(ctx, <primary_key>=row_key)`；`primary_key` 参数名来自 schema，值来自当前选中行。Contracts 补齐 `PageSchema` 及页面组件/action/CRUD TypedDict，并且 Hosted UI schema 不再接受 `Button.action.type="page_action"`。定向回归已覆盖 Contracts schema、根导出和 ManagedPageRenderer CRUD。
- 最新修复：2026-05-11 已收口 Hosted UI 内联 `DataTable` 查询契约。`crawler4j-contracts` 新增并根导出 `HostedDataTableQuery`、`HostedDataTableQueryResult[RowT]`、`HostedDataTableSortSpec`；`ModuleUIRuntimeBridge.call_query_handler()` 现在只按 `(context, query)` 调用模块函数，不再把 `table_id` 或独立 `params` 传给业务 handler；`ManagedPageRenderer` 从 `DataTable.columns` 中显式 `searchable=True` 的列推导 `query.search_fields`，只允许显式 `sortable=True` 的列进入 `query.sort`，未声明列默认不可搜索、不可排序，并过滤非法 sort 字段，强制 query handler 返回 `HostedDataTableQueryResult`，不再接受普通 `dict`，结果也不再回传 `sort`。`DataTable.table_id` 保留为页面内 UI 组件实例 ID，可用于宿主渲染/刷新定位，但不再表达数据库资源名；页面导航参数统一进入 `query.params`。本次 `sortable` 默认值补充回归 `104 passed`，目标 `ruff check` 与 `git diff --check` 通过。
- 最新修复：2026-05-11 已收口 `TaskContext` 边界：Contracts 删除 `screenshot()`、`run_subtask()` 和 `_subtask_executor`，保留由 Core MMS 注入的 `run_page_action()`；Core 侧核查未发现真实截图工具实现，只有 REM 能力示例注释提到 screenshot，现已改成不暗示已实现截图。`run_subtask` 在 Core 中没有执行器注入路径，本轮清理为 contracts/测试/文档残留删除；`run_page_action` 的真实路径仍在 `packages/crawler4j/src/core/mms/service.py` 注入 `_page_action_executor` 并按 descriptor 调用 `@page_action`。定向回归 `31 passed`，目标 `ruff check` 与 `git diff --check` 通过。
- 最新修复：2026-05-11 已修正 `ctx.db.from_(...).execute()` 默认分页语义。Contracts 现在只有显式调用 `.limit(...)` 或 `.offset(...)` 才在查询计划里写入分页值；Core SQL 渲染在缺省分页时不再追加 `LIMIT/OFFSET`，会读取满足条件的全部行。新增回归覆盖 managed_dataset 与 custom_table 超过 100 行的默认全量读取，并同步开发者指南与设计口径。定向回归 `121 passed`，目标 `ruff check` 与 `git diff --check` 通过。
- 最新修复：2026-05-11 已修正 ATM 运行模板对象装配树的展示行为。公共 `ObjectGraphTree` 现在按当前可见树节点动态收缩/展开高度，避免少量对象图节点时出现大块空白；展开/收起按钮改为组件自绘浅色 chevron，不再沿用 Qt 原生深色 branch indicator。定向回归：`uv run pytest packages/crawler4j/tests/unit/test_ui/test_object_graph_tree.py -q` => `3 passed`，`uv run pytest packages/crawler4j/tests/unit/test_core/test_atm/test_run_profile_dialog.py -q` => `32 passed`；目标 `ruff check` 通过。
- 最新修复：2026-05-11 已调整 ATM 运行模板候选参数配置窗口的空值展示。空 `candidate_params` 现在打开时显示为空白编辑器，不再默认显示 `{}`；留空保存仍解析为空字典，已有非空参数继续按 YAML 对象展示。用户指南、候选队列设计与运行模板 UI 单测已同步。
- 最新开发：2026-05-09 已为 ATM 运行模板选择环境模式补齐 `candidate_params` 图形配置入口。运行模板表单现在在候选函数下方显示“候选参数”配置窗口，使用 YAML 对象编辑并校验，保存后写入 `RunProfile.resource.acquisition.candidate_params`；从已有运行模板加载再通过表单保存不会再丢失候选参数，调试会话继续从运行模板派生同一份 payload。用户指南、候选队列设计与开发者指南已同步，运行模板 UI 单测覆盖加载保留与 YAML 校验。
- 最新开发：2026-05-09 已为 `ctx.db.into(...).delete_where` 与 `ctx.db.batch().delete_where` 增加主键值快捷删除。模块可写 `delete_where(where="A001")`；Contracts 会先读取数据源描述，`custom_table` 映射到 `record_key_field`，`managed_dataset` 映射到宿主物理 `record_key`，普通结构化 where 与 callable where 保持原语义。开发者指南已同步，Contracts plan 与 Runtime capabilities 定向测试已覆盖。
- 最新开发：2026-05-09 已为 `ctx.db.into(...).update_where/delete_where` 与 `ctx.db.batch().update_where/delete_where` 增加 callable `where` 构造入口。模块可写 `where=lambda q: q.where("id", "=", value)` 复用现有 `DatabaseQueryBuilder.where(...)` 条件语法；callable 只收集 where 条件，不触发额外数据源描述，也会拒绝未添加任何条件的更新/删除。开发者指南已同步，Contracts plan 单测与 Runtime capabilities 定向测试已覆盖。
- 最新开发：2026-05-09 已为 `ctx.db` 增加公开 `describe(source)` 数据源描述入口。`source` 使用 `@data_table/@data_view` 逻辑名，宿主返回归一化后的 `kind/source_kind/storage_mode/record_key_field/columns/system_fields/writable_fields/required_fields/read_only_fields`。`custom_table` 非自增主键会作为必填可写字段返回，自增主键作为只读生成字段返回；`managed_dataset` 业务字段在 `columns`，`record_index/record_key/run_status/record_status/created_at/updated_at` 在 `system_fields`，其中只有 `run_status/record_status` 可写。新增 Contracts 与 ModuleDataStore 定向覆盖，开发者指南已同步。
- 最新开发：2026-05-09 已为 `managed_dataset` 增加 `where` 后行数统计。`ctx.db.from_("accounts").where(...).count(alias="total").execute()` 对托管快照表返回 `[{"total": n}]`，where 继续支持 schema 业务字段和 `run_status` / `record_status` 等宿主物理字段；该能力只开放单个 `count(*)` aggregate，不支持 `join`、`group_by`、混合列选择或 `sum/avg/min/max`。新增 ModuleDataStore 与 Runtime capabilities 定向覆盖。
- 最新开发：2026-05-08 已为 `custom_table` 增加自增主键支持。`@data_table.schema` 可在 `record_key_field` 对应的 integer 字段上声明 `auto_increment=True`，Core 建表会生成 `INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT`；新增 `ctx.db.into(...).add(...)` 和 `ctx.db.batch().add(...)` 作为 insert-only 写入入口，自增表新增时可省略 id 并返回生成 id 列表，`upsert` 仍要求稳定主键。定向回归新增覆盖 Contracts metadata/校验、manifest registry 同步、`ModuleDataStore` 建表与插入、Runtime capabilities 路由和 `ctx.db` plan。
- 最新开发：2026-05-08 已新增 `@ui_action` 作为 Hosted UI 用户操作正式入口。Contracts 根导出 `ui_action`，SDK scanner/CLI/manifest lock 识别 `ui_action` 并新增 `crawler4j ui-action create/list`；Core v2 descriptor 记录 `ui_actions`，`ModuleUIRuntimeBridge.call_ui_action` 和 `ManagedPageRenderer` 支持 `Button.action.type="ui_action"`。`@page_action` 边界同步收紧为 workflow/component 调用的浏览器页面动作，运行时拒绝 `page_action -> page_action` 嵌套调用，公共步骤应抽到 helper、browser adapter 或 use case。`docs/03-developer-guide/v0.4.0/architecture-rules.md` 已新增模块 DDD 与固定目录规则。定向回归：`ui_action/page_action` 相关 SDK、Contracts、Hosted UI、MMS runtime/renderer 聚焦测试 `89 passed`，目标 ruff 与 `git diff --check` 通过。
- 最新开发：2026-05-08 已新增 `@data_view` 作为只读数据库视图契约，并删除旧查询注解入口、运行时入口和兼容诊断路径。Contracts 根导出 `data_view`，SDK scanner/CLI/模板/manifest lock 识别 `data_view`；Core v2 descriptor、MMS registry 与 `ModuleDataStore` 会把 `@data_view` 同步为真实 SQLite view，运行时统一通过 `ctx.db.from_("view_id")` 只读访问。normalized data contract 只保留 `resources/views/seeds`；`@data_view` 与 legacy data.views 都只允许引用 `custom_table`。定向回归：SDK/Contracts/CLI 48 passed，Core 数据链路 126 passed，MMS/ATM 空 data contract 120 passed，`ModuleDataStore` 60 passed；目标 ruff 与 `git diff --check` 通过。
- 最新开发：2026-05-08 已按 schema-only 语义完成 `managed_dataset` 重构。`@data_table.schema` 作为稳定字段声明、空表描述基线和 `record_json` 持久化白名单，replace/upsert/update_where 只会把 schema 业务字段合并进 `record_json`；查询字段描述只展开 schema 字段与 `module_datasets` 物理列。`record_key` / `record_index` / `created_at` / `updated_at` 由宿主管理，replace/upsert 传入会忽略以支持读后写回；`run_status` / `record_status` 允许在 replace/upsert/update_where 中作为物理状态列写入，不进入 `record_json`。
- 最新开发：2026-05-08 已继续删除数据库视图/查询列契约里的冗余查询能力开关。Core MMS data contract、`ModuleDataStore` DB view schema 归一化和 `crawler4j-contracts` Hosted UI DB view schema 都不再接受 `filterable` / `sortable`；筛选、排序统一按 `ctx.db` / 内部查询执行器的字段存在性和数据源类型校验处理。UI `DataTable` 列的 `sortable` 仍作为前端展示配置保留，不属于数据库查询契约。
- 最新开发：2026-05-08 已继续按重构方案删除模块数据读取薄 wrapper。`ModuleDataStore.query_resource_records()` 是资源读取主入口，`execute_query_plan()` 是 view / relation 查询入口，旧 `get_record`、`list_records`、`read_resource_records`、`query_db_view` 以及对应内部 helper 已删除。`ctx.db.from_` 不再自行解析资源 schema，而是通过 `ModuleDataStore.describe_data_source()` 复用内部查询同一份字段描述；`managed_dataset` 会通过 SQLite 查询把宿主物理列与 schema 业务字段展开到同一行记录，schema 字段过滤和排序继续下推 SQLite `json_extract(...)`，并发写入策略保持现有 `DbWriteCoordinator` 不变。
- 最新开发：2026-05-08 已按重构方案收口 `ctx.db` 条件 API：查询统一使用数组条件 `where(["age", "=", 18])`，`select(["A", "B"])` 支持字段数组，`["and", ...]` / `["or", ...]` 支持多重嵌套组合；`where_gt/where_in/where_like` 等公开变体已从 contracts 层移除，内部候选查询改走统一 `where(...)`。`managed_dataset` 增量写已补齐 `upsert/update_where/delete_where`；replace/upsert/update_where 会合并 schema 业务字段到 `record_json`，并可写 `run_status` / `record_status` 物理状态列；`update_where` 继续拒绝 `record_key` / `record_index` / `created_at` / `updated_at` 生成型物理字段。并发处理保持现有 `DbWriteCoordinator` 策略；`data.db` 的 `module_datasets` / `module_db_views` / `module_data_resources` 旧 schema 迁移保护已删除，启动时只接受 0.4.0 当前结构。
- 最新修复：2026-05-08 已把 `managed_dataset` 的 `ctx.db.from_(...).where/select/order_by` 查询路径明确拆成两类字段：`module_datasets` 物理列 `record_index` / `record_key` / `run_status` / `record_status` / `created_at` / `updated_at`，以及通过 SQLite `json_extract(record_json, '$.<field>')` 查询的 schema 顶层业务字段。schema 外 JSON key 不会被查询表达式补进描述，`managed_dataset` 仍禁止 join/group/非 count 聚合。定向回归：`uv run pytest packages/crawler4j/tests/unit/test_core/test_persistence/test_module_data_store.py -q` => `60 passed`。
- 最新开发：2026-05-02 已把 ATM 运行模板对象装配区收敛为公共 `ObjectGraphTree` 树形组件。UI 现在按 `workflow -> interface 绑定行 -> 子 interface/参数` 展示，绑定行左侧显示 interface 中文 `label(name)`，右侧下拉框显示 component 中文 `label(name)`，不再额外显示“注入对象”行；interface 下拉继续保存到 `RunProfile.execution.object_bindings`，绑定行下的 `object_param(...)` 创建参数继续保存到 `RunProfile.execution.object_params`；Workflow 仍不接收普通参数。定向回归：`uv run pytest packages/crawler4j/tests/unit/test_ui/test_object_graph_tree.py packages/crawler4j/tests/unit/test_ui/test_component_imports.py packages/crawler4j/tests/unit/test_core/test_atm/test_run_profile_dialog.py -q` => `30 passed`。
- 最新开发：2026-05-02 已把标准拟人化页面交互正式收口为宿主 `browser.*` tool 协议。Core/ATM 新增 `browser.pause/goto/move/click/hover/type/press/drag/scroll/page_down/page_up`，运行时只在当前 `TaskContext` 绑定可用 `page` 时暴露这些工具；`TaskContext` 与 `ModuleUIRuntimeBridge` 会自动把当前上下文绑定给 tools。SDK 脚手架、0.4.0 开发者指南与设计文档已统一改为“模块直接 `ctx.tools.call("browser.*", ...)` 执行标准页面交互，`ctx.page` 主要保留给读取和宿主未覆盖能力”。后续评分优化已把拟人化行为补强为宿主侧可测试模型：分段停顿、idle/导航扫描漂移、目标尺寸感知轨迹、mouse/key dwell、自然输入纠错概率、敏感输入默认不纠错、惯性滚动与 trace 摘要。定向回归：`uv run pytest packages/crawler4j/tests/unit/test_core/test_atm/test_browser_tools.py packages/crawler4j/tests/unit/test_core/test_atm/test_runtime_capabilities.py packages/crawler4j/tests/unit/test_sdk/test_taskcontext.py -q` => `59 passed`；`uv run pytest packages/crawler4j/tests/unit/test_core/test_mms/test_module_ui_runtime.py -q` => `8 passed`；目标 `ruff check` 通过。
- 最新开发：2026-05-01 已按确认方案完成 `ctx.db` 并发写入底座第一阶段重构。模块开发者 API 保持兼容，新增 `ctx.db.into(...).upsert/update_where/delete_where(...)` 与 `ctx.db.batch().execute()`；宿主新增 `DbWriteCoordinator`，对 `data.db` 写入设置 busy timeout、`BEGIN IMMEDIATE` 短事务、进程内 writer/resource 锁和 SQLite busy 重试，写入成功自动提交、异常自动回滚。`replace()` 继续保留全量覆盖语义，文档已明确并发任务更新同一实体表时优先使用 `custom_table` 的增量写 API 或 batch。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_sdk/test_data_capability.py packages/crawler4j/tests/unit/test_core/test_persistence/test_module_data_store.py packages/crawler4j/tests/unit/test_core/test_atm/test_runtime_capabilities.py -q` 为 `81 passed`。
- 最新审查：2026-05-01 已完成 0.4.0 全面 code review / 修复 / 测试闭环。审查发现并修复：`RUNTIME_SURFACE_FULL` 不再暴露旧 `ui.declare_page` / `ui.get_page` 持久宿主页声明路径；空 workflow 不再被强制写成 `"default"`，由 v2 descriptor 的单 workflow / main workflow 规则解析；`Job.params` 已标注为 legacy metadata 且不再作为运行时注入入口；开发者指南和根文档入口已把 0.4.0 标为当前主线、0.3.x 标为历史维护；UI smoke 改为跟随真实 Shell 导航项数量。验证：`uv run pytest -q -p no:cacheprovider` => `886 passed`，`uv run ruff check .`、`git diff --check`、`uv run build`、`uv run python -m crawler4j_sdk.cli.commands --help`、`uv run python scripts/smoke_test_ui.py`、`uv run package-desktop` 均通过；本地 macOS 产物为 `packages/crawler4j/dist/desktop/macos/Crawler4j.app`。生产发布结论仍为 No-Go：`ctrip` 真实站点 DevLink + ZIP 双链 E2E、Windows 真机签名/安装/自更新和正式交付批次尚未闭环。
- 最新开发：2026-05-02 已补齐 workflow/object 运行前生命周期。`crawler4j-contracts` 新增公开 `WorkflowLifecycleInfo`，`TaskOutcome` 扩充 `workflow` 元信息；workflow/component 可选实现 `setup(ctx, workflow)` 与 `cleanup(ctx, outcome)`，component 不强制实现生命周期方法。Core 在对象图构造后按 component 组合顺序再到 workflow 调用 setup，然后执行 `workflow.run(ctx)`；setup 失败不会执行 run，但会生成 failed outcome 并进入 cleanup；终态 cleanup 顺序为 component 依赖反向顺序后再 workflow。SDK scanner 新增 `V2_OBJECT_LIFECYCLE_METHOD_SIGNATURE_INVALID` 诊断，校验 setup/cleanup 签名，旧 `aclose()` / `close()` 仍报 `V2_OBJECT_LIFECYCLE_METHOD_UNSUPPORTED`。定向回归：SDK + MMS 单测 `374 passed`，目标 `ruff check` 与 `git diff --check` 通过。
- 最新修复：2026-05-01 已补齐 SDK 对 `managed_dataset` 托管快照系统字段的前置阻断。`HOST_RESERVED_DATA_FIELDS` 继续只表示全局宿主保留字段：真实物理时间列 `created_at` / `updated_at` 以及常见误写 `create_at` / `update_at`；新增 `MANAGED_DATASET_RESERVED_DATA_FIELDS` 表示 `managed_dataset` 额外禁止声明的 `record_index` / `record_key` / `run_status` / `record_status`。SDK scanner 仅在 `storage_mode="managed_dataset"` 时把这组字段纳入 schema、类注解和 indexes 诊断，`custom_table` 不受额外限制。开发者指南、需求文档与测试记忆已同步。
- 最新开发：2026-05-01 已完成 0.4.0 审查修复闭环：`RunProfile.execution.params`、ATM/Debug `execution_params` / `job_params` / `runtime_params` 注入链和 Hosted UI `ctx.runtime["params"]` 均已退出正式运行面；运行模板只保存 `object_bindings` / `object_params`，模块 workflow 通过宿主装配对象接收依赖。2026-05-01 生命周期收口后进一步移除了旧 debug session 重建表迁移与旧 job `run_profile_json` 字段归一，只保留当前 schema 路径。定向回归 `176 passed`，全量回归 `886 passed`，目标 ruff、全包 ruff 与 `git diff --check` 通过。
- 最新开发：2026-05-01 已完成 0.4.0 数据表事实源收口补强：`module.yaml.data` 继续作为已移除 manifest 字段被拒绝，Core 同步数据契约不再回退到旧 manifest data 路径；`crawler4j-contracts @data_table` 新增 `storage_mode` / `record_key_field` / `cleanup_policy` metadata，SDK scanner、CLI 模板与 Core registry 已按装饰器 metadata 生成运行时数据契约。`@data_table` 默认保持当前 v2 行为为 `custom_table`，旧快照语义必须显式写 `storage_mode="managed_dataset"`，并继续落到 `data.db.module_datasets`；只读视图由 `@data_view` 声明并只能引用 `custom_table`。开发者指南、设计文档与测试记忆已同步。
- 最新开发：2026-04-30 已按 0.4.0 破坏性原则清理旧生命周期 hook 运行链。Core/ATM/Debug 已删除 `RunProfile.execution.hooks_module`、`ExecutionRequest.hooks_module`、`DebugSession.hooks_module`、`ModuleService.call_hook()` / `call_local_hook()`，`ExecutionRunner` 不再调用 `prepare_env/init_env/before_run/on_success/on_failure/on_timeout/on_cleanup`，配置中心也移除了终态 hook / cleanup hook 超时项；模块流程控制只通过 workflow 主体返回 `TaskResult`，后续对象生命周期收口为可选 `setup(ctx, workflow)` 与 `cleanup(ctx, outcome)`，环境回收继续由宿主执行。Hosted UI 同步调用入口已从 `call_local_hook` 改名为 page action 语义。聚焦回归 `113 passed`，全量 `uv run pytest packages/crawler4j/tests -q` => `881 passed`。
- 最新开发：2026-04-30 已新增 0.4.0 批量环境清理候选契约。模块通过 `cleanups/*.py` 中的 `@env_cleanup_candidates` 同步纯函数返回 env id 列表或 `EnvCandidates` 链式查询；清理候选运行面为只读 `ctx.db`、无 tools。Core descriptor、SDK scanner/CLI 模板、MMS resolver 与 REM `EnvCleanupService` 已接入独立 `env_cleanup_candidates` bucket，不复用 `@env_candidates` 调度入口；环境管理页新增“批量清理”按钮，先扫描预览和确认，再二次校验 `READY/PAUSED`、无租约、无关联任务后调用 `EnvironmentManager.destroy_env()`。开发者指南与 API 设计已同步；聚焦回归 `123 passed`，目标 ruff 与 `git diff --check` 通过。
- 最新开发：2026-04-30 已按 0.4.0 正式口径把环境选择从资源池同步方案改为纯函数候选方案。模块目录统一使用短目录 `candidates/`，通过 `@env_candidates` 同步纯函数返回 env id 列表或 `EnvCandidates` 链式查询；账号黑号、注册时间、会员等级等业务过滤由候选函数实时读取模块数据表，不维护宿主资源池快照。Core/ATM/MMS/REM/UI 已删除模块侧 `resource_pool` 能力面、资源池可用状态列和相关事件，运行模板只接受 `env_id` 或 `candidates`；后续批量清理候选通过独立 `cleanups/` 契约接入。最终验证：`uv run pytest packages/crawler4j/tests -q` => `884 passed`；`uv run ruff check packages/crawler4j packages/crawler4j-sdk packages/crawler4j-contracts` 与 `git diff --check` 均通过。
- 最新开发：2026-04-30 已完成 0.4.0 破坏性重构首轮开发-测试-审查闭环。最终范围包括：ATM/Debug 移除 `selector_name/env_selector` 运行路径并强制 `candidates/env_id`；Contracts 根导出保留 `@page`、移除 `PageSpec/specs`；SDK/CLI 清除 `module.yaml.ui_extension` 页面登记兼容路径；Hosted UI `page_action` 改为 kwargs 调用并限制到 `hosted_ui_action` 能力面；Core v2 workflow 在异常路径也会恢复 `_page_action_executor`；DevLink 才强制 reload descriptor；外部模块 manifest lock 对真实模块文件保持 symlink 拒绝，忽略目录内的本地开发 symlink 由 2026-05-30 修复改为先跳过；`ObjectContainerV2` 会拒绝拼写错误的 `object_bindings` 注入路径。最终验证：`uv run pytest packages/crawler4j/tests -q` => `874 passed`；`uv run ruff check packages/crawler4j packages/crawler4j-sdk packages/crawler4j-contracts` => 通过。
- 最新开发：2026-04-30 已完成 0.4.0 Hosted UI 破坏性收口到注解模式。`crawler4j-contracts` 新增根导出 `@page(...)` 并移除 `PageSpec` 公共入口；SDK/CLI/manifest lock/Core v2 descriptor 已把 `pages/` 纳入装饰器扫描，`module.yaml.ui_extension` 被视为已移除字段；页面菜单改由 `@page(menu=True)` 控制，`menu=False` 页面仍可通过 `open_page.page_id` 路由。开发者指南、Hosted UI 设计文档与 memory 已同步到注解模式。阶段性验证：SDK 全量单测 `174 passed`，Contracts/scanner/Core v2 descriptor 聚焦 `24 passed`，SDK CLI/MMS manifest 聚焦 `44 passed`，Hosted UI runtime/detail/renderer/card/scroll 拆分回归 `42 passed`，CLI integration + acceptance `18 passed`，相关目录 `ruff check` 与 `git diff --check` 通过。
- 最新开发：2026-04-30 已扩展 0.4.0 v2 component 对象参数类型。`ParameterSpec` 新增 `schema` / `item_schema`，`object_param(...)` 支持 `string/text/integer/number/boolean/enum/array/object/json/date/datetime/time/url/path/secret`；Contracts 运行时注解和 SDK 静态 scanner 可从 `Literal[...]`、`list[T]`、`dict[str, T]`、`Optional[T]` / `T | None`、`datetime.date/datetime/time`、`pathlib.Path` 推断类型；Core `ObjectContainerV2` 会校验并归一 ISO 日期/时间、路径、数组、对象、URL、secret 和 JSON-like 值。开发者指南、SDK/Contracts README、SDK 模板与 memory 已同步。阶段性验证：聚焦 v2 contracts/scanner/container `41 passed`，Core v2 descriptor/container `27 passed`，相关文件 `ruff check` 与 `git diff --check` 通过；后续 Hosted UI 注解模式收口已覆盖当时的页面契约漂移。
- 最新开发：2026-04-30 已补齐 0.4.0 v2 对象装配的类属性注解与 `__init__` 参数注解入口。`crawler4j-contracts` 新增并导出 `object_param(...)` / `object_inject(...)`，会把类属性或构造函数参数上的 `Annotated[..., object_param/object_inject]` 归一到既有 `ParameterSpec` / `InjectSpec`；SDK v2 scanner 静态解析同一语法并写入 manifest lock；Core descriptor/container 使用归一后的元数据完成对象注入和对象参数校验。装饰器参数写法继续兼容。本轮验证：SDK 单测 `171 passed`、Core v2 descriptor/container `20 passed`、acceptance `19 passed`、SDK CLI integration `11 passed`、相关目录 `ruff check` 与 `git diff --check` 通过。
- 最新文档：2026-04-30 已完成开发者指南版本分流和 0.4.0 开发版正文编制。`docs/03-developer-guide/index.md` 现在只做版本选择；0.3.0 稳定指南已冻结到 `docs/03-developer-guide/v0.3.0/`；0.4.0 开发者指南已写入 `docs/03-developer-guide/v0.4.0/`，固定讲 `core-native-v2` 装饰器对象装配、workflow 无 parameters、component object params、`@page_action`、`@data_table/@data_view`、manifest lock、SDK/Contracts/Core 边界、`ctx.db` 唯一数据库入口和宿主保留字段诊断。0.4.0 文档口径已收口为 SDK / Contracts / Core 按产品线绑定，0.4.x SDK / Contracts 只服务 Core 0.4.0，不兼容 0.3.x 命令、模板或开发模式。根 `docs/index.md` 与 `.factory/memory/doc-map.md` 已同步开发者指南版本路径。
- 最新开发：2026-04-30 已完成 0.4.0 SDK / Contracts / Core 破坏性升级首轮实施：源码版本线统一为 `crawler4j 0.4.0`、`crawler4j-sdk 0.4.0`、`crawler4j-contracts 0.4.0`；Contracts 根导出 v2 装饰器并移除根导出的 `TaskSpec/WorkflowSpec/EnvSelectorSpec`；SDK CLI、模板、scanner、`check full`、manifest lock、package build/verify 已切到 `core-native-v2`，不再暴露 0.3.x `task/env-selector/hook/data resource` 主命令；Core MMS 已新增 `ModuleRuntimeDescriptorV2/load_runtime_descriptor_v2`，Core `ModuleScanner` / DevLink / install preflight 已接受 v2 清单并拒绝旧 `module.yaml.data/workflows/default_workflow` 运行事实源；`ObjectContainerV2` 已具备单次对象图装配、实例隔离和 object params 类型/enum/min/max/未知字段校验。当前阶段门禁 `acceptance + unit SDK + Core v2 descriptor/container` 为 `202 passed`，ruff 与 `git diff --check` 通过。本分支只服务 Core 0.4.0，旧 SDK / Contracts 版本在 0.3.x 分支维护。
- 最新开发：2026-04-30 `crawler4j module init` 已改为易用默认入口。新手可直接执行 `uvx --from crawler4j-sdk crawler4j module init`，交互式输入模块包名与升级源仓库；`version`、`workflow_name`、`python_version`、`runtime_api=core-native-v2` 等非必填项使用默认值。资深开发者仍可一次性传参执行完整命令。本轮最新验证：SDK/CLI/脚手架 `185 passed`、acceptance `19 passed`、Core v2/MMS/ATM 聚焦 `63 passed`、debug E2E `4 passed`，ruff、`git diff --check`、`uv lock --check` 均通过。
- 最新设计：2026-04-30 已形成使用者指南和开发者指南的 docs-stratego 版本分流方案。后续只对 `docs/02-user-guide/` 和 `docs/03-developer-guide/` 做版本化，根目录只保留版本选择页，正文进入 `v0.3.0/`、`v0.4.0/` 等目录；docs-stratego 网站主文档必须指向当前已发布版本，老版本保留为历史版本入口，0.4.0 未发布前只能作为开发版预览。内部 `04-project-development/` 不随版本复制。
- 最新设计：2026-04-30 已形成 0.4.0 装饰器对象装配正式方案。`core-native-v2` 目标契约将运行能力事实源从 `module.yaml` 对象图切到代码装饰器：`@interface/@component/@workflow/@page_action/@data_table/@data_view`；workflow 不再声明 parameters，只通过构造函数接收宿主装配对象；对象参数只属于 component 创建；task 退化为页面操作纯函数；Core 负责按运行模板为每个 task/env 创建独立对象图，SDK/Contracts 负责装饰器、扫描、校验、迁移和 manifest lock；SDK 模块打开、DevLink、`check full` 与打包阶段必须前置阻断 `created_at` / `updated_at` / `create_at` / `update_at` 等宿主保留或混淆数据库字段。对应需求与架构文档已新增到 `docs/04-project-development/{03-requirements,04-design}`。
- 最新开发：2026-04-30 已完成 4.0 首个新需求的 Workflow 运行参数与运行模板 UI。该实现保留为当前 0.4.0 分支已提交代码事实，但后续正式架构方向已调整为 `core-native-v2` 装饰器对象装配，workflow parameters 不再作为 v2 正式契约。聚焦回归 `81 passed`，全量回归 `828 passed`。
- 历史同步：2026-04-29 曾将桌面宿主源码版本线提升到 `crawler4j 0.3.2` 并产出 wheel/sdist；该版本卡已被 2026-04-30 `0.4.0` SDK / Contracts / Core 同步升级覆盖，0.3.x 后续在 0.3.x 分支维护。
- 最新修复：2026-04-29 已修正在线模块升级在弱网/代理场景下留下半截 ZIP 且只弹空白 warning 的问题。根因不是 GitHub Release 资产本身损坏，而是 `ModuleReleaseService._download_release_asset()` 复用了共享 `aiohttp` session 的 `30s total timeout`；下载超时时会抛空消息 `TimeoutError`，同时把已经写入一半的资产直接留在 `downloads/modules/.../*.zip`。当前模块升级下载已改为独立 `connect/read` 超时、写入 `.part` 临时文件并在超时/截断时清理残留，超时会明确提示“下载模块安装包超时，请检查网络或代理设置后重试”，`content-length` 不匹配时会直接拒绝不完整安装包。同时 `ModuleListWidget` 升级失败若遇到空消息异常，会回退到诊断面板而不是继续弹空白 warning；`ModuleDetailPage._test_repo_token_async()` 也已切到 `MessageDialog.warning_async/information_async`，避免 repo token 测试先把 qasync 事件循环打坏，再连带影响后续升级操作。
- 最新修复：2026-04-29 已修正 Windows 宿主模块配置页里 QScintilla YAML 编辑器的字形挤压问题。此前 `YamlCodeEditor` 把编辑字体硬编码为 `Menlo`，在 Windows 上会退回到不可控字体，配合固定 3px 额外行高后容易出现配置键值和相邻行视觉上挤在一起。当前共享 YAML 编辑器已改为按平台可用固定宽字体优先级选择 `SF Mono/Menlo/Consolas/Cascadia Mono/Monaco/JetBrains Mono/Courier New`，找不到时再退回 Qt 系统固定宽字体；额外行高也改为按字体 `lineSpacing()` 动态计算。新增 focused 回归锁定字体选择顺序与实际 extra ascent/descent，模块配置页和运行模板 YAML 编辑器都会一起受益。
- 最新修复：2026-04-29 已修正任务监控页在手动批次 `run_once` 预检查失败后的 qasync 重入问题。此前 `TaskListWidget._async_op()` / `_async_stop_run_once()` / `_async_create_job()` / `_async_update_job()` 这些异步协程在失败分支里会直接弹 `MessageDialog.*.exec()` 同步对话框；当 `VirtualBrowser` 路径未配置等预检查错误触发时，同一回调里又会刷新表格和统计，嵌套 Qt 事件循环会把 `Dashboard`、`Shell`、`TaskListWidget` 自己的后台任务同时拉进来，最终刷出 `Cannot enter into task` 和 `Task was destroyed but it is pending`。当前这些异步失败弹窗已统一改走 `warning_async/error_async`，避免在 qasync 协程里再开阻塞式 `exec()`；新增任务列表 focused 回归锁定 `run_once` / `stop_run_once` 失败路径只允许使用异步弹窗。
- 最新修复：2026-04-29 已修正 Service Job 暂停后残留资源池等待席位的问题。此前固定环境池作业在没有可分配工位时会把 task 持久化为 `PENDING + waiting_since`，随后 dispatcher loop 结束并清掉运行时引用；用户暂停 job 时，统一 stop 入口只能打断 live task，无法触达这类“已脱离运行时、但仍未终态”的 pending task，于是仪表盘会把它继续计入 `运行中`。当前 `TaskRepository` 新增“取消未拿到环境和 lease 的 pending task”收口 helper，`TaskDispatcher.request_stop_for_job()` 在处理 live task 后会直接把这类等待席位写成 `CANCELLED`，清空 `waiting_since`，结果文案固定为 `Job paused before resource acquired`，并补发 `TASK_CANCELLED` 事件；对应 dispatcher / job modes 回归已通过。
- 最新修复：2026-04-29 已为 ATM `on_cleanup` 收尾补上 stop 状态兜底。此前模块在 cleanup 中继续调用 `ctx.run_subtask()` 时，会因为 `TaskContext.request_stop()` 已设置停止标记而抛出 `asyncio.CancelledError`，该异常不属于普通 `Exception`，会逃逸并阻断后续环境处置，导致 VirtualBrowser 环境无法回收到 READY。历史说明：该轮曾通过 `on_cleanup` 兜底 stop 收尾；2026-04-30 已删除 0.4.0 生命周期 hook 运行链，后续对象生命周期由可选 `setup(ctx, workflow)`、`cleanup(ctx, outcome)` 与宿主环境回收处理。
- 最新修复：2026-04-29 已修正“打开 DEBUG 后日志疯狂刷屏导致桌面应用卡死”的根因。问题不是业务代码死循环，而是统一日志服务把 root logger 一起切到 `DEBUG` 后，`qasync` 线程执行器、`asyncio`、`httpx/httpcore`、`urllib3` 等第三方库的内部调试日志也全部进入文件日志与 Dashboard 实时日志；其中 `qasync` 还会把 `TaskRepository.list_jobs/count_active_tasks` 的 Future result 和整段 `Job(...)` repr 打出来，单条日志可达 13KB，历史轮转文件曾涨到 5.99GB。当前 `src.core.foundation.logging` 已为这些第三方 logger 增加 `WARNING` floor，保留 `crawler4j` 自身 DEBUG 能力，同时新增 `test_debug_level_keeps_crawler4j_debug_but_suppresses_qasync_noise` 回归锁定该行为；定向回归 `test_logging.py`、`test_log_console.py`、`test_app.py`、`test_config_center.py` 与目标文件 `ruff check` 已通过。
- 最新修复：2026-04-28 已把左侧一级 `关于` 页重构为左对齐结构化布局，不再复用居中式关于弹窗内容。当前页面顶部展示宿主图标、产品名、版本与 Build，下面按“更新 / 自动检查更新 / 更新操作”两行排列公共 `ToggleSwitch` 与更新按钮，继续保持暗色主题且不使用卡片套卡片。
- 最新修复：2026-04-28 已落地宿主通用配置中心，删除旧 `PreferencesService`、`ConfigStore`、`SettingsPage` 与 `settings` 表。宿主全局配置现在由 `ConfigCenterService` + `config.db.config_entries` 管理，配置中心 UI 按 schema 渲染 `系统 / 网络 / 外部浏览器 / 任务运行 / 资源`，模块业务配置继续保留在 MMS `module_config_entries`；客户端自身版本信息与宿主更新入口已收口到左侧一级 `关于` 页。旧 `settings` 表启动时一次性迁移到 `config_entries` 后删除；开机自启动与启动隐藏到托盘配置已从 UI/schema/启动流程中删除，历史 `system.autostart` / `system.minimize_on_start` 配置会在初始化时清理；ATM 收尾预算改为配置中心可配；2026-04-30 已删除终态 hook 与 cleanup hook 预算，2026-05-01 当前只保留环境回收超时，新建任务默认主体执行超时仍为 `600s`。运行模板表单已新增 `主体执行超时` 字段，和资源选择 `等待超时` 分离；长任务配置口径为只调整运行模板主体执行超时（`0` 表示不自动中断主体），不关闭宿主收尾保护；配置中心布尔开关已固定公共 `ToggleSwitch` 尺寸，不再被表单宽度拉伸。
- 最新修复：2026-04-27 已修正 Windows 宿主自更新的 Velopack 安装态误判。根因不是用户没有通过 `Setup.exe` 安装，而是宿主错误依赖了 Velopack Python binding 并未暴露的 `is_installed` 属性，导致即便从 `Setup.exe` 创建的快捷方式启动也会被误判为“未通过 Setup 安装”。当前 `src.core.system.velopack` 改为按官方发布布局识别 Velopack 正式产物：`Setup.exe` 安装态使用 `current/sq.version + ../Update.exe`，`Portable.zip` 使用 `./Update.exe + ./current/sq.version`；两类正式产物都允许 `检查更新`，裸 `PyInstaller onedir` 仍明确判为不支持。新增 `test_velopack.py` 覆盖安装态、Portable 与裸 bundle 三条回归，并同步修正文档口径。
- 最新修复：2026-04-27 已把 IP 池条目“测试”操作切到公共 `ProgressDialog`，不再复用 `SkyDataTable` 顶部细进度条伪装“处理中”。`IPPoolTab` 工具栏上的“新建池 / 刷新 / 添加 IP / 批量导入”也已从局部 `QPushButton` 样式收口到公共 `StyledButton`。同时完成一轮公共 UI 组件审查；旧 `settings_page.py` 后续已由 2026-04-28 `config_center_page.py` 替代。定向回归 `14 passed`，目标文件 `ruff check` 通过。
- 最新修复：2026-04-27 已完成表格行内按钮被挤压/裁切的二次修复。根因不是单个按钮宽度，而是 `QTableWidget::item { padding: 10px; }` 会压缩 `setCellWidget()` 的内容矩形；44px 行高下 action cell 实际只剩约 22px，高度 34px 的中文/emoji 按钮必然被裁剪。当前 `SkyDataTable` 在存在 actions 列时默认行高提升到 52px、item 垂直 padding 收敛到 6px，并在渲染每一行时显式 `setRowHeight()`；`create_action_button()` 保持 34px 固定高度并垂直居中。复测几何为行高 52px、action cell 38px、按钮 34px；聚焦回归 `12 passed`，相关 UI 回归 `53 passed`，`uv run ruff check .` 与 `git diff --check` 通过，全量 `uv run pytest -q -p no:cacheprovider` 为 `777 passed`。
- 最新同步：2026-04-27 开发团队 4 已同步版本与证据口径：当前源码版本线为 `crawler4j 0.3.1`、`crawler4j-sdk 0.6.1`、`crawler4j-contracts 0.4.0`，最近正式 Git tag 仍为 `v0.2.0`。该条已被 2026-04-29 的 `0.3.2` 版本同步更新覆盖；`docs/02-user-guide/assets/screenshots/` 下的大量截图资产仍仅登记为后续文档资产清理候选，本轮不删除、不移动。
- 最新修复：2026-04-27 已在保持正确数组缩进的前提下恢复模块配置 YAML 配色。`YamlCodeEditor` 现使用自定义 `YamlDisplayLexer`，不再依赖会画坏 sequence 缩进的 `QsciLexerYAML`；当前 key 为暖黄、字符串为绿色、数字为蓝色、`true/false/null` 等字面量为橙色，数组缩进仍按真实空格显示。定向回归 `22 passed`，目标文件 `ruff check` 通过。
- 最新修复：2026-04-27 已确认模块配置页数组缩进“文本正确但显示错位”的根因来自 `QScintilla + QsciLexerYAML`。离屏渲染验证表明编辑器底层文本已是 `      - item`，但 YAML lexer 会把 block sequence 的可视缩进吃掉；当前 `YamlCodeEditor` 已停用 `QsciLexerYAML`，保留纯 `QScintilla` 渲染、行号、校验标记、15pt 字号、额外行高与滚动条隐藏，数组项现在按真实空格位置显示。定向回归 `21 passed`，目标文件 `ruff check` 通过。
- 最新修复：2026-04-26 已继续优化模块配置 YAML 可读性。此前数组缩进异常来自 PyYAML 默认 `indentless sequence` 输出风格，当前 `ModuleConfigPage._dump()` 改用自定义 SafeDumper，数组保存/展示为父 key 下缩进的 `- item`；`YamlCodeEditor.setPlainText()` 也会兜底规范化旧的 `key:\n- item` 文本；编辑器字号提升到 15pt，并增加额外行高。定向回归 `21 passed`，目标文件 `ruff check` 通过。
- 最新修复：2026-04-26 已收口模块详情页滚动条与 YAML 编辑器视觉。`YamlCodeEditor` 现在隐藏横向/纵向滚动条，折叠边栏从树状连线改为 plain fold，并弱化缩进参考线；模块详情页左侧菜单、任务链页面、Hosted 页面滚动区与 `SkyDataTable` 也统一隐藏滚动条但保留滚轮/触控板滚动。定向回归 `22 passed`，目标文件 `ruff check` 通过。
- 历史记录：2026-04-26 曾跟进资源池声明契约调整 ATM 客户端交互，运行模板从 `module.yaml.resource_pools[]` 加载资源池下拉。该口径已被 2026-04-30 `@env_candidates` 纯函数候选方案取代，当前 `module.yaml.resource_pools[]` 是已移除字段。
- 历史记录：2026-04-26 曾把固定环境池名称收口到模块清单 `resource_pools[]`。该中间方案已被当前环境候选契约取代；宿主不再注入 `ctx.runtime["declared_resource_pools"]`，运行模板只接受固定 `env_id` 或已声明 `candidates`。
- 最新修复：2026-04-26 已彻底统一任务启动进度处理。宿主新增 Shell 级 `TaskProgressPresenter`，统一订阅 `TASK_PROGRESS` / `TASK_STARTED` / 任务终态事件并展示全局 `ProgressDialog`；ATM 手动“执行一次”只发布 `requesting` 进度事件，不再持有页面私有启动弹窗；REM “从已有环境导入并执行”不再轮询 task 的 `PENDING -> RUNNING`，只提交导入与关联任务，导入队列剩余环境通过 `queued` 进度事件持续暴露，后台补发任务也会刷新统一排队进度。用户在携程数据采集等导入场景看到的是同一套任务进度弹窗与任务监控行内状态，不再是 REM 的“环境操作中/正在启动导入环境窗口”旧路径。聚焦回归 `59 passed`，ATM/REM/UI 宽回归 `351 passed`，全量 `uv run pytest -q` 为 `747 passed`，`uv run ruff check .` 与 `git diff --check` 通过。
- 最新修复：2026-04-26 已重构“从已有环境导入并执行”语义。导入对话框现在选择已配置的“批次任务 + 执行一次”Job，不再按每个导入环境创建 `已有环境导入/...` 临时 Job；一次可选择多个来源环境，每个环境作为关联 Job 下的一条 Task 运行实例，初始派发与后续补发都受该 Job 的 `concurrency_target` 限制。Core 传给模块的 `ctx.runtime["creation_params"]` 继续提供 `provider/name/provider_env_id/provider_env_name/import_mode`，与 `ctrip_crawler import_env_workflow` 的导入元信息契约对齐，避免导入已登录环境时报 `import_metadata_missing`。
- 最新修复：2026-04-25 公共 `MessageDialog` 已按“安装模块”对话框视觉重做为原生窗口壳 + `#1e1e28` 深色内容面板，内部动作按钮改用公共 `StyledButton(success/secondary)`；“安装模块”面板自身也把输入框、浏览/取消/开始检查按钮和校验提示收口到公共 `StyledLineEdit` / `StyledButton` / `MessageDialog`。同步审查 UI 弹窗使用后，已将 `TaskCreateDialog`、`RunProfileDialog`、`JobDebugDialog`、`TaskListWidget` 的简单提示/确认，MMS 配置/详情/Hosted CRUD 提示，Settings 重置确认与 EditEnv 结果提示切到公共 `MessageDialog` / `ConfirmDialog`。剩余 `QMessageBox` 集中在 `EnvListWidget`、`ModuleListWidget` 的异步 `open()` 流程和 `TaskListWidget` 的三按钮中止决策，需要后续补公共异步/多动作弹窗能力后再替换。聚焦回归 `80 passed`，更宽 UI/MMS/ATM/System 相关回归通过。
- 最新修复：2026-04-25 IP 测试结果弹窗已从局部 `QMessageBox` 收口为公共 `src.ui.components.message_dialog.MessageDialog`；`IPPoolTab` 内的导入完成/错误提示与删除确认也改用公共 `MessageDialog` / `ConfirmDialog`。同时 `state.db` 初始化不再创建 `env_ip_bindings`，IP 池绑定只维护当前进程内的 env->ip 映射并直接增减 `ip_entries.bound_count`；`config.db` 也不再创建遗留 `configs` 表，已有库清理交给显式 SQL。聚焦回归 `18 passed`。
- 最新修复：2026-04-25 运行环境列表已合并 `env_metadata` 并展示资源池可用状态。列表加载线程会读取每个环境的元数据，按 `scheduler.resource_pool` 资格卡片聚合为 `可用 / 部分可用 / 不可用 / 未标记`，并把模块名、资源池名和不可用原因放入 tooltip/search 文本；IP 池条目测试结果弹窗已改为深色背景，避免 macOS 下浅色面板导致文字不可读。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_core/test_rem/test_env_list_widget.py packages/crawler4j/tests/unit/test_core/test_rem/test_ip_pool_tab.py` 为 `20 passed`。
- 最新修复：2026-04-25 已收口 VirtualBrowser 端口错误/API 不可达时“像卡住”的启动与环境操作路径。REM 启动恢复、GC 与创建失败清理现在对指纹浏览器运行时只做短超时探活，不再为未完成/错误环境按 30s 等待；ATM active job bootstrap 也改用短 runtime timeout，避免主窗口显示前被 active job 串行阻塞。环境列表创建/启动/销毁/导入来源列表现显示明确 loading/status 文案，导入来源 API 报错会弹出可见错误；Playwright CDP 连接超时从 300000ms 降为 15000ms；配置中心 VirtualBrowser 默认端口为 9002。定向回归 `82 passed`，更宽核心单测 `uv run pytest packages/crawler4j/tests/unit/test_core -q` 为 `469 passed`，目标文件 `ruff check` 通过。
- 最新修复：2026-04-24 已补齐 SDK `check full` 对 `data.resources[]` 的未知字段校验。`module.yaml.data.resources[]` 资源项公开字段只允许 `id/storage_mode/record_key_field/schema/indexes/cleanup_policy/joins`，`resource_id` 仅是 Core 规范化后的内部字段，不允许出现在原始 manifest 资源项中；SDK 生成命令已由测试显式锁定只写 `id`。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_sdk/test_cli_scaffold.py -q` 为 `23 passed`，目标文件 `ruff check` 通过。
- 最新修复：2026-04-24 数据库模块专项 code review 已完成闭环。审查团队确认旧 `db.*` 工具面未再挂到模块运行时，但发现 SDK `check full` 对换行旧调用扫描不足、contracts 根导出测试漂移、SQL 模板可读取未声明表、聚合 fluent 默认 alias 不可用、空 `managed_dataset` 单条读取和 SQL/seed 路径边界存在隐患；当前已完成专项修复，`ctx.tools.call/has_tool("db.*")` 改为 AST 扫描阻断，view/query 执行加入 SQLite authorizer 限制只读已声明物理表，`sum/avg/min/max` 默认生成 `<func>_<field>` alias，路径越界改用 `Path.relative_to()`。同时修复破坏性升级暴露的 debug/dev-link 测试清单缺 `data` / `runtime_api` / `workflows`、固定环境池测试 mock 缺 `run_env_selector`、`crawler4j-contracts 0.4.0` 依赖与 README/release 口径漂移。全量回归 `uv run pytest -q -p no:cacheprovider` 为 `694 passed`，`uv run ruff check .` 通过，`uv lock --check` 通过。
- 历史记录：2026-04-24 曾修正 Hosted UI 页面注册与左侧菜单协议，使用 `PageSpec` 和 `module.yaml.ui_extension.pages[]` 区分可路由页面与菜单页面。当前 0.4.0 已收口到 `@page(...)` 注解模式，`PageSpec` 与 `module.yaml.ui_extension` 不再属于正式契约。
- 最新验证：2026-04-24 已按正式方案把模块数据库开发者接口收口为唯一 `ctx.db` fluent API。`crawler4j-contracts` 新增 `DatabaseClient`，ATM / Hosted UI 运行上下文注入 `ctx.db`，旧 `ctx.tools.call("db.*")` 工具面已不再注册给模块；`managed_dataset` 仅支持单源读取，`custom_table` 支持 manifest 声明后的 join/group/aggregate，视图保持只读查询并统一走 `ctx.db.from_("view_id")`。SDK `check full` 已新增旧数据库工具调用扫描，开发者指南、设计契约与 `.factory/memory` 已同步到新口径。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_sdk/test_taskcontext.py packages/crawler4j/tests/unit/test_sdk/test_data_capability.py packages/crawler4j/tests/unit/test_sdk/test_cli_scaffold.py packages/crawler4j/tests/unit/test_core/test_persistence/test_module_data_store.py packages/crawler4j/tests/unit/test_core/test_atm/test_runtime_capabilities.py packages/crawler4j/tests/unit/test_core/test_atm/test_execution_runner.py packages/crawler4j/tests/unit/test_core/test_mms/test_module_ui_runtime.py packages/crawler4j/tests/unit/test_core/test_mms/test_managed_page_renderer.py packages/crawler4j/tests/unit/test_core/test_mms/test_settings_store.py -q` 为 `159 passed`，目标 Python 文件 `uv run ruff check ...` 通过。
- 最新修复：2026-04-24 已修复 IP 池条目编辑“当前页已变、重启后恢复旧值”的持久化缺陷。根因是 `IPPoolManager._persist_entry()` 写 `state.db.ip_entries` 时，`ON CONFLICT(id) DO UPDATE` 只更新了 `bound_count/safety_score/expires_at`，没有回写 `address/protocol/port/username/password/pool_id`；当前已补齐这些字段的 upsert 更新，新增 `uv run pytest packages/crawler4j/tests/unit/test_core/test_rem/test_ip_pool.py -q` 锁定“编辑后重载数据库仍保留新代理字段”。
- 最新修复：2026-04-24 已为 `环境管理 -> IP 池管理` 的 IP 条目补齐“真实代理测试”入口。当前条目操作栏新增 `测试` 按钮，宿主会按条目协议真实走 `HTTP/HTTPS/SOCKS4/SOCKS5` 代理访问出口 IP 探针服务，并在弹窗中直接返回是否成功、出口 IP、耗时与失败阶段；结果只用于即时诊断，不写回数据库。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_core/test_rem/test_proxy_probe.py packages/crawler4j/tests/unit/test_core/test_rem/test_ip_pool_tab.py -q` 已通过。
- 最新修复：2026-04-24 已把模块管理页“安装失败”提示从只显示 `str(e)` 的通用消息框收口为专用诊断弹窗。当前宿主会在安装失败时直接展示 `message / stage / hint / traceback`，并支持复制完整诊断文本，因此即便异常本身没有 message，也不会再出现空白告警；定向回归 `uv run pytest packages/crawler4j/tests/unit/test_core/test_mms/test_module_list_widget.py -q` 与目标文件 `ruff check` 已补进本轮验证口径。
- 最新修复：2026-04-24 已继续收口“从已有环境导入”弹窗的风险提示。此前 warning 区域虽然已补齐高度同步，但用户仍反馈存在额外内圈且文案表达过重；当前 `packages/crawler4j/src/core/rem/ui/import_existing_env_dialog.py` 已把 warning card 改成单层提示卡，内层文字显式固定为 `background: transparent; border: none; padding: 0;`，并把 warning 文案收敛为“该 workflow 未标注支持‘已有环境导入’，请由配置者自行判断是否适合这个场景”。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_core/test_rem/test_import_existing_env_dialog.py -q` 为 `1 passed`，目标文件 `ruff check` 通过；本地 Qt 直出截图 `/tmp/import-existing-env-dialog-fix-v2.png` 已确认无额外内圈。
- 最新修复：2026-04-24 已修复桌面宿主启动/关闭阶段的入口竞态。`packages/crawler4j/src/ui/app.py` 现把 debug worker/debugpy adapter/Shell 等深依赖收口为运行时懒加载，避免打包态在启动早期因循环导入直接退出；GUI 生命周期则保持 `quitOnLastWindowClosed(False)`，改由 `lastWindowClosed` 触发 `_run_application()` 的异步收尾，不再在窗口关闭时先停事件循环再抛 `Event loop stopped before Future completed`。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_ui/test_app.py -q` 为 `6 passed`，目标文件 `ruff check` 通过，`uv run package-desktop` 产出的 macOS `.app` 已能越过原先的启动即退阶段。
- 最新修复：2026-04-24 已为宿主全局环境页补齐“从已有环境导入”能力，2026-04-25 已按最新业务口径删除 provider 扩展字段并把唯一性收口到 `(provider, name)`。`state.db.environments` 与 `Environment` 模型不再保存额外 provider 元数据；`VirtualBrowser` 未同步列表用来源名称在本地不存在来判断，环境页允许选择来源、模块、workflow 和未同步环境后执行 `导入并执行`。ATM 现支持固定 `env_id` 直接运行模块 workflow，模块可从 `ctx.env_id`、`ctx.page` 与 `ctx.runtime["creation_params"]` 读取 `provider`、`name` 和 `import_mode="existing_env"`。
- 最新修复：2026-04-24 已清理桌面打包链路的遗留资源引用。此前 `packages/crawler4j/crawler4j.spec` 仍把已经删除的 `src/ui/styles/dark_theme.qss` 当作 PyInstaller data 收集项，导致 Windows `package-windows-release` 在 data append 阶段直接失败；现已移除该死引用，并把 `test_packaging_config.py` 扩展为显式锁定 spec 不再引用 `dark_theme.qss` 或 `src/ui/styles`。同时已把 README、release 文档与 `.factory` 版本事实源统一到 `crawler4j 0.3.1` / 最近正式 tag `v0.2.0`。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_sdk/test_packaging_config.py packages/crawler4j/tests/unit/test_sdk/test_icon_packaging.py packages/crawler4j/tests/unit/test_core/test_mms/test_removed_runtime_surface.py -q` 为 `71 passed`，`uv build --package crawler4j --out-dir /tmp/crawler4j-build-check` 产出 `crawler4j-0.3.1` wheel/sdist，`uv run package-desktop` 本机复验通过。
- 最新修复：2026-04-24 已继续按反馈收口桌面图标中心主组。当前 `scripts/rebuild_app_icon_assets.py` 在保持外底板 inset `(97, 96, 97, 96)` 不变的前提下，把圆形镜面保持在白底板几何正中，并将手柄从“短尾巴”延长到更接近右下边缘的安全区，但仍保留明显留白，不贴边、不越界；镜面继续保持约占底板宽度的 `80%`。`test_icon_packaging.py` 继续锁定蓝色主徽记 bbox `(210, 210, 815, 815)`、左右上下 margin 仅允许 `1px` 误差，以及整组 focus bbox `(210, 210, 846, 850)`。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_sdk/test_icon_packaging.py -q` 为 `6 passed`，对应 `ruff check` 通过。
- 最新验证：2026-04-24 已为共享 `Card` / Hosted UI `Card` 补齐布局参数。当前 `packages/crawler4j/src/ui/components/card.py` 已支持 `title_align`、`content_align`、`content_vertical_align`、`min_height`、`padding`，`crawler4j-contracts` 的页面 schema 归一化与 `ManagedPageRenderer` 也已同步透传这些字段，使模块页面可直接声明“标题居中 / 内容居中 / 垂直居中 / 最小高度 / 统一内边距”。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_sdk/test_hosted_ui_card.py packages/crawler4j/tests/unit/test_core/test_mms/test_managed_page_card.py packages/crawler4j/tests/unit/test_ui/test_card.py packages/crawler4j/tests/unit/test_ui/test_stat_card.py -q` 为 `7 passed`，对应 `ruff check` 已通过。
- 最新修复：2026-04-24 已修正共享 `Card` 的样式作用域错误。此前 `StyleSheets.card()` 使用通配 `QFrame` 选择器，Qt 中 `QLabel` 继承自 `QFrame`，导致卡片标题与副标题也被套上了卡片边框，表现为“文字外面再包一层框”；现已改为只命中 `QFrame#sharedCard`，并补充 `test_card.py` 回归锁定该选择器不再回退。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_ui/test_card.py packages/crawler4j/tests/unit/test_ui/test_stat_card.py packages/crawler4j/tests/unit/test_ui/test_dashboard.py -q` 为 `5 passed`，对应 `ruff check` 已通过。
- 最新验证：2026-04-24 已把“卡片”正式收口为共享容器语义。`packages/crawler4j/src/ui/components/card.py` 现提供统一原生 `Card` 外壳，`packages/crawler4j/src/ui/components/stat_card.py` 已改为基于 `Card` 组合标题/数值/变化值，不再绘制模板式内层框；`crawler4j-contracts` 新增 Hosted UI `type="Card"` schema，`ManagedPageRenderer` 已支持直接渲染 `Card`，且历史 `Section.variant="card"` 现走同一底层组件作为兼容别名。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_sdk/test_hosted_ui_card.py packages/crawler4j/tests/unit/test_core/test_mms/test_managed_page_card.py packages/crawler4j/tests/unit/test_ui/test_card.py packages/crawler4j/tests/unit/test_ui/test_stat_card.py packages/crawler4j/tests/unit/test_ui/test_dashboard.py packages/crawler4j/tests/unit/test_ui/test_component_imports.py -q` 为 `9 passed`，`uv run pytest packages/crawler4j/tests/unit/test_core/test_mms/test_managed_page_renderer.py packages/crawler4j/tests/unit/test_core/test_mms/test_module_detail_page.py -q` 为 `17 passed`，对应 `ruff check` 已通过。
- 最新验证：2026-04-24 已清理 UI 目录中的静态孤岛资源。经全仓引用扫描确认，`packages/crawler4j/src/ui/styles/dark_theme.qss` 没有任何加载入口，而 `packages/crawler4j/src/ui/assets/arrow_down.svg` / `arrow_up.svg` 已被 `StyledComboBox` / `StyledSpinBox` 的代码绘制箭头完全取代；本轮已删除上述 3 个死资源，并把 `packages/crawler4j/tests/unit/test_core/test_mms/test_removed_runtime_surface.py` 扩展为同时锁定这些旧资源路径不存在。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_core/test_mms/test_removed_runtime_surface.py packages/crawler4j/tests/unit/test_ui/test_form_controls.py packages/crawler4j/tests/unit/test_ui/test_component_imports.py -q` 为 `6 passed`，对应 `ruff check` 通过。当前 `packages/crawler4j/src/ui/` 目录内除图标资源链外，暂未检出下一批同级强证据孤岛文件。
- 最新验证：2026-04-24 已继续收口桌面 UI 孤岛代码，并新增正式统计卡组件。经目录级引用扫描确认，`packages/crawler4j/src/ui/components/config_editor.py`、`log_viewer.py`、`status_bar.py` 均无任何业务入口引用，且 `config_editor.py` 的唯一依赖 `packages/crawler4j/src/ui/utils/syntax_highlighter.py` 也随之成为孤岛；本轮已将它们删除，并新增 `packages/crawler4j/src/ui/components/stat_card.py` 作为统一的标题/数值/变化值卡片组件，`packages/crawler4j/src/ui/dashboard.py` 已改为复用该正式组件。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_ui -q` 为 `28 passed`，`uv run pytest packages/crawler4j/tests/unit/test_core/test_mms/test_removed_runtime_surface.py -q` 为 `3 passed`，`uv run ruff check packages/crawler4j/src/ui/dashboard.py packages/crawler4j/src/ui/components/stat_card.py packages/crawler4j/src/ui/theme/styles.py packages/crawler4j/tests/unit/test_ui/test_stat_card.py packages/crawler4j/tests/unit/test_ui/test_dashboard.py packages/crawler4j/tests/unit/test_ui/test_component_imports.py packages/crawler4j/tests/unit/test_core/test_mms/test_removed_runtime_surface.py` 通过。
- 最新验证：2026-04-24 已清理 UI 组件目录中的旧路径残留。未被任何正式入口消费的 `packages/crawler4j/src/ui/components/metric_badge.py` 仍引用已删除的 `src.ui.widgets.glass_card`，现已连同未使用的 `glass_card.py` 与对应 `StyleSheets.GLASS_CARD` 一并删除，并新增 `packages/crawler4j/tests/unit/test_ui/test_component_imports.py` 作为目录级导入 smoke，要求 `src.ui.components` 下保留模块全部可导入。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_ui -q` 为 `26 passed`，`uv run ruff check packages/crawler4j/src/ui/theme/styles.py packages/crawler4j/tests/unit/test_ui/test_component_imports.py` 通过，实施计划与当前记忆已同步更新。
- 历史记录：2026-04-24 曾补齐 Hosted UI 行内 CRUD 适配，并把数据写入交给模块 `hooks/*.py`。当前 0.4.0 正式链路已删除 lifecycle/local hook 运行面，Hosted UI 用户操作通过 `@ui_action` 与受限能力面收口；浏览器页面动作才使用 workflow/component 调用的 `@page_action`，数据读写边界以 `ctx.db` 和页面 handler 契约为准。
- 最新发布：2026-04-24 已将 `crawler4j-sdk` 提升到 `0.5.2` 并发布到 PyPI。本轮同时纳入宿主页源码分组、`module repair-init` 命令与 Hosted UI 管理资源 CRUD 表格改动；定向回归 `uv run pytest packages/crawler4j/tests/unit/test_core/test_mms/test_managed_page_renderer.py packages/crawler4j/tests/unit/test_sdk/test_cli_scaffold.py packages/crawler4j/tests/unit/test_sdk/test_packaging_config.py -q` 为 `87 passed`，`uv run ruff check packages/crawler4j-contracts/src/hosted_ui.py packages/crawler4j-sdk/src/cli/commands.py packages/crawler4j/src/core/mms/ui/managed_page_renderer.py packages/crawler4j/tests/unit/test_core/test_mms/test_managed_page_renderer.py packages/crawler4j/tests/unit/test_sdk/test_cli_scaffold.py packages/crawler4j/tests/unit/test_sdk/test_packaging_config.py` 通过，`uv run build crawler4j-sdk` 与 `uv run publish crawler4j-sdk` 均已成功完成。
- 最新验证：2026-04-24 已补齐模块根包恢复命令。`crawler4j-sdk` 新增 `crawler4j module repair-init`，可按当前标准模板单独重建模块根 `__init__.py`，用于清理旧 `run()` / `declare_ui()` 残留或误删后的恢复；该命令只覆盖根包文件，不会改动 `module.yaml`、任务、工作流、Hook、环境选择器或页面源码。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_sdk/test_cli_scaffold.py -q` 为 `19 passed`，`uv run ruff check packages/crawler4j-sdk/src/cli/commands.py packages/crawler4j/tests/unit/test_sdk/test_cli_scaffold.py` 通过。
- 最新验证：2026-04-24 已完成宿主页源码物理分组协议收口。`packages/crawler4j/src/core/mms/runtime_descriptor.py` 现支持从 `pages/` 递归发现宿主页模块，`crawler4j-sdk` 的 `check full` 改为以 `PAGE.id` 对齐 `ui_extension.pages[]` 而不再要求页面文件名等于 page id，CLI 新增 `crawler4j page create <page_id> --group <group>` 用于把同一左侧菜单下的多文件页面落到 `pages/<group>/`；页面路由与 `open_page.page_id` 仍保持扁平。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_sdk/test_cli_scaffold.py packages/crawler4j/tests/unit/test_core/test_mms/test_module_ui_runtime.py packages/crawler4j/tests/integration/test_sdk_cli_module_mode.py packages/crawler4j/tests/acceptance/test_sdk_cli_scaffold_package_acceptance.py -q` 为 `38 passed`，开发者指南、SDK README 与 `.factory/memory` 已同步更新。
- 最新验证：2026-04-23 已完成 `managed_dataset` 资源注册链路最终收口。`packages/crawler4j/src/core/persistence/module_data_store.py` 现删除“未声明资源也能直接落到 `module_datasets`”的读写 fallback，ATM 暴露给模块的 `db.get_record` / `db.list_records` / `db.replace_records` 也已同步删除历史 `dataset=` 别名并统一到 `resource=`；模块记录读写现在与 `custom_table` 一样都必须先命中 `module.yaml.data.resources[]`。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_core/test_persistence/test_module_data_store.py packages/crawler4j/tests/unit/test_core/test_atm/test_runtime_capabilities.py packages/crawler4j/tests/unit/test_core/test_mms/test_module_ui_runtime.py packages/crawler4j/tests/unit/test_core/test_mms/test_managed_page_renderer.py packages/crawler4j/tests/unit/test_core/test_mms/test_settings_store.py packages/crawler4j/tests/unit/test_sdk/test_data_capability.py -q` 为 `94 passed`，目标 Python 文件 `uv run ruff check ...` 通过。
- 最新同步：2026-04-23 已完成开发者指南与用户指南的口径回写。`docs/03-developer-guide/` 现统一补记 `module.yaml.data + data/sql + data/seeds`、CLI `data` 命令组，以及 `db.get_record` / `db.run_query` / `db.query_view` 的正式数据边界；`docs/02-user-guide/` 的管理员与异常分诊页现同步提示旧协议模块包会被宿主直接拒绝加载，便于现场优先判断交付包批次与协议兼容性。
- 历史记录：2026-04-23 曾按 `module.yaml.data + data/sql + data/seeds` 形成模块数据契约升级草案，包含资源、视图和查询命令。当前 0.4.0 正式链路已改为 `@data_table/@data_view` + manifest lock，运行时代码统一通过 `ctx.db` fluent API 访问。
- 最新验证：2026-04-23 已完成 `crawler4j-contracts` / `crawler4j-sdk` 对外发布专项审阅闭环。审查团队、开发团队与测试团队复核后确认：SDK 版本解析已不再依赖相邻源码目录，CLI `check/package/release` 与 `core-native-v1` 模块边界已对齐，SDK/Root 对 contracts 的依赖范围已收口为 `>=0.3.0,<0.4.0`，脚手架 README / workspace README / 发布脚本顺序与当前版本基线已统一到 `crawler4j 0.3.0`、`crawler4j-sdk 0.5.0`、`crawler4j-contracts 0.3.0`。主线程定向回归 `120 passed`，`ruff check` 与 SDK/Contracts build dry-run 通过。
- 最新验证：2026-04-23 已完成代码洁净性专项审查第三轮收口。4 个审查子 agent 与主线程复核确认：`crawler4j-sdk/src/hosted_ui.py` 的 Hosted UI 归一化 helper 属于真实阻塞级重复实现，现已收口为对 `crawler4j_contracts.hosted_ui` 的兼容薄 re-export，并新增 `test_hosted_ui_reexport.py` 锁定 SDK/Contracts 同源实现；`src/core/system/update_service.py` 中未被消费的 `UpdateInfo` 占位 dataclass 已删除；ATM 两个测试文件里对 `normalize_db_view_schema` 的过时兼容垫片已清理。裁决同时保留了旧安装目录迁移回滚、repo-token 旧命名别名、workflow 旧字段与默认工作流 fallback 等兼容/契约面问题，未在本轮贸然删除。定向回归 `47 passed + 17 passed`，目标文件 `uv run ruff check ...` 通过。
- 最新验证：2026-04-23 已按方案 A 完成 `core-native-v1` 硬切换。Core 现为唯一运行时 owner，`packages/crawler4j/src/core/mms/runtime_descriptor.py` / `service.py` / `module_ui_runtime.py` 已改为宿主侧扫描并承载任务、工作流、Hook、环境选择器与页面描述；`crawler4j-contracts` 新增 `TaskSpec` / `WorkflowSpec` / `EnvSelectorSpec` / `PageSpec` 与 Hosted UI 归一化 helper，`crawler4j-sdk` 删除 `ModuleAssembler` / `TaskScript` / `TaskFlow` / `env_selector` 等运行时导出并切到 contracts-only 模板。主线程组合回归 `158 passed`，开发者指南、README 与 `.factory/memory` 稳定摘要已同步到“模块运行时代码只依赖 contracts，旧模块无兼容桥直接拒绝”口径。
- 最新验证：2026-04-23 已完成 `crawler4j-contracts` / `crawler4j-sdk` 对外接口专项审阅收口。审查团队与主线程复核确认本轮需修两类问题：一是 CLI `check full` 的 `declare_ui` 校验面必须与 core `RUNTIME_SURFACE_HOSTED_UI_DECLARE` 完全一致，当前已收口为只允许 `ui.declare_page`，并补齐 `env-selector list` 导入失败 fail-fast 与模块脚手架 `dependency-groups.dev`（`pytest` / `pytest-asyncio`）默认生成；二是 `TaskContext.screenshot()` 同名同秒截图覆盖风险与 `crawler4j-contracts` 根导出 / `DefaultHttpClient` 行为回归已补锁定。主线程定向回归 `uv run pytest packages/crawler4j/tests/unit/test_sdk/test_taskcontext.py packages/crawler4j/tests/unit/test_sdk/test_contracts_exports.py packages/crawler4j/tests/unit/test_sdk/test_cli_scaffold.py packages/crawler4j/tests/integration/test_sdk_cli_module_mode.py packages/crawler4j/tests/acceptance/test_sdk_cli_scaffold_package_acceptance.py -q` 为 `109 passed`，目标文件 `uv run ruff check ...` 通过；另一次 `uv run python -m crawler4j_sdk.cli.commands --help` 在当前沙箱命中 `~/.cache/uv` 权限限制，未作为代码阻塞项判定。
- 最新验证：2026-04-23 已完成 UI 模块专项审阅收口。审查团队确认真实阻塞项收敛为两类：一是 Hosted UI 运行时必须保持纯 UI 边界，`declare_ui` / page / query handler 不得经宿主能力做任何数据写入，且 `ManagedPageRenderer.refresh()` 现只消费本轮 `declare_ui()` 的内存声明缓存，不再回退到 `module_pages` 持久化 schema；二是 SDK/CLI 必须统一拒绝 legacy hosted UI artifacts，`check full` / `package build` / `package verify` / `page create` 现全部阻断 `ui/`、`config_schema.json`、`strategy.yaml` 与 legacy `ui_extension` 字段。定向回归 `uv run pytest packages/crawler4j/tests/unit/test_core/test_atm/test_runtime_capabilities.py packages/crawler4j/tests/unit/test_core/test_mms/test_module_ui_runtime.py packages/crawler4j/tests/unit/test_core/test_mms/test_managed_page_renderer.py packages/crawler4j/tests/unit/test_sdk/test_cli_scaffold.py packages/crawler4j/tests/integration/test_sdk_cli_module_mode.py packages/crawler4j/tests/acceptance/test_sdk_cli_scaffold_package_acceptance.py -q` 为 `119 passed`。
- 最新验证：2026-04-23 已完成数据模块专项审阅收口。该轮首先把 `db.query_view` 的只读视图边界与 `declare_ui` 无副作用约束收紧；随后同日晚些时候又被新的 manifest 驱动协议取代，当前正式口径以本页最上方“模块数据契约破坏性升级第二轮收口”为准。
- 最新验证：2026-04-23 开发团队 2 已补齐 SDK 数据能力契约测试覆盖；该批次最初锁定的是旧 `db.declare_*` 公开面，现已被同日晚些时候的新契约测试替换为 `db.get_record` / `db.list_records` / `db.replace_records` / `db.run_query` / `db.query_view`。
- 历史记录：2026-04-23 曾将 Hosted UI 收口为 `ui_extension.pages[]` 导航元信息和 `ui.declare_page` / `ui.get_page`。当前正式契约已改为 `pages/` 下 `@page(...)` 注解声明，`module.yaml.ui_extension` 与 `ui.declare_page` 不再是 0.4.0 主线。
- 最新修复：2026-04-23 已完成 `CR-014` / `TASK-028` 初版实现：宿主新增 `module_db_views` 与 `db.query_view` 只读统计表链路；该轮最初仍含运行时 `db.declare_db_view` 声明口，现已被后续 manifest 驱动协议替代。
- 最新修复：2026-04-23 已完成 `CR-013` / `TASK-027` 最终收口：Hosted UI 新增 `open_page.params`、表格 `row_action`、模块详情页缓存页参数替换，以及目标页面内联表格的 `navigation_filters`；默认 CRUD 已改为对底层全量资源做定点写回，不再在过滤详情表里覆盖 sibling records，`row_action` 未声明 `params` 时也不会再透传整行；同时保留显式 `data_resource` metadata 与 omitted `resource_id` 的 alias 路由兼容。当前主表点击可打开关联详情页/详情表，目标页收到最新 params，目标详情表会按参数过滤关联记录；对应最终定向回归 `122 passed`、目标文件 `ruff check` 通过，测试团队结论 `PASS`、审查团队结论 `no blocking findings`。
- 最新修复：2026-04-22 已继续微调桌面图标的壳层占比：共享图标母版的导出缩放系数现从 `0.965` 下调到 `0.964`，重新生成 `app_icon.png` / `app_icon.icns` / `app_icon.ico` 后，macOS Dock 与 Windows 壳层图标再缩小约 1px。
- 最新修复：2026-04-23 已完成 `CR-012` / `TASK-026` 纠偏收口：宿主新增 `module_data_resources` 统一登记 `managed_dataset` / `custom_table`，`module_datasets` 升级到带 `record_key` / `run_status` / `record_status` 的 V3 结构，`custom_table` 已从泛型 `record_json` 容器改为 `schema_version` / `schema_json` / `indexes_json` 驱动的受控实体表；该轮最初仍使用运行时 `db.declare_data_resource` 登记资源，现已被后续 manifest 驱动协议替代。
- 最新修复：2026-04-23 已继续按根因收口桌面图标的外底板占比：在保持蓝色放大镜主徽记 bbox 不变的前提下，`scripts/rebuild_app_icon_assets.py` 现把外底板进一步缩到 inset `(97, 96, 97, 96)`，用于把 Dock 里真正偏大的白色外轮廓再收 1px，压回到更接近系统常见图标的占比。
- 最新修复：2026-04-23 已继续收口 Windows 打包版首窗启动竞态：`src.ui.app:_run_application` 在 `Shell.show()/showMinimized()` 后不再立刻恢复 `quitOnLastWindowClosed`，而是先让 `qasync` 事件循环过一个 tick 再恢复，避免 Windows 安装态在主窗口完成首轮原生注册前就提前停掉事件循环；对应 `test_app.py` 已同步锁定这个顺序。
- 最新修复：2026-04-22 已继续收口桌面应用图标的 Dock 外轮廓占比：仓库现新增 `app_icon_master.png -> scripts/rebuild_app_icon_assets.py -> app_icon.png/icns/ico` 的固定生成链，运行时与打包图标统一按同一比例回缩到更保守的四边安全区；同时图标回归测试已修正右/下 inset 计算口径，并锁定四边安全区在同一范围内，避免图标再次在 macOS Dock 中显得比浅色系统图标更大。
- 最新修复：2026-04-22 已完成 hosted UI V1 第二轮收口：模块详情页入口改为首次选中时再实例化，未选中的宿主页不会提前执行 `declare_ui()` 或 `load_handler`，且已有 hosted page 在再次选中菜单时会自动 refresh；`ModuleUIRuntimeBridge` 现把 `declare_ui` 产出的 session 收口为“下一次 non-declare hook 单次消费”，并通过 staging buffer + `replace_declared_ui()` 保证 UI schema 整套原子替换，`declare_ui` 失败时旧 schema 保留、新 schema 不会半写入；`crawler4j check full` / `package build` / `package verify` 同步补齐 `load_handler` 与内联表格 `query_handler` 的存在性、同步性与签名兼容校验，`ui/` 目录也已正式收口为 legacy 结构并在打包/发布链路阻断。
- 历史记录：2026-04-22 曾完成 hosted module UI V1 主链，使用 `ui_extension.pages[]`、`ui.declare_page` / `ui.get_page` 与 `data.db.module_pages` 持久化页面 schema。当前 0.4.0 正式契约已迁移到 `@page(...)` 注解和 v2 descriptor，页面 schema 不再以 `module.yaml.ui_extension` 或 `data.db.module_pages` 为事实源。
- 最新修复：2026-04-22 已继续修正 Windows Velopack CLI 在 `uv run python` 下的两段断链：当 `dnx` 或 `vpk` 实际解析为 `*.cmd` / `*.bat` 时，`package-windows-release` 现在会自动改用 `cmd.exe /c` 调起 CLI；若 Python `velopack` 版本带 `.dev` / `+local` 后缀，则会先正规化成 NuGet 可接受的基础版本后再传给 `dnx --version`，不再继续命中 `WinError 2` 或 `invalid NuGet version`。
- 最新修复：2026-04-22 已继续修正 Windows 打包版首窗显示前的启动竞态与安装态图标漂移：`src.ui.app:main` 现在会在异步启动阶段先禁用 `quitOnLastWindowClosed`，并在主窗口 `show()` 后继续把恢复动作延后到下一轮事件循环，避免 Windows 客户端在 `Shell` 真正完成首轮显示前提前停掉 `qasync` 事件循环；同时 `package-windows-release` 现会把 `app_icon.ico` 继续传给 Velopack，确保 `Setup.exe`、安装后快捷方式与 `Crawler4j.exe` 使用同一套 Windows 图标。
- 最新修复：2026-04-22 已继续收口桌面宿主应用图标的 Dock 光学尺寸：当前正式图标仍保留“暖灰白底板 + 蓝色主徽记 + 放大镜轮廓 + 4j 主字标”的浅色品牌母版，但当前正式收口口径已改为只按外白底板的光学占比调节，蓝色主徽记作为稳定内层不再继续追着缩；当前直接锁定该回归的是 `test_icon_packaging.py` 的“透明圆角 + 中轴安全区 + 浅色暖灰底板 + 蓝色主徽记 + 中心主标”断言。
- 最新修复：2026-04-22 已把桌面壳层图标链路继续补齐到跨平台同源：当前共享母版仍是浅色品牌 `app_icon.png`，macOS bundle 使用同源导出的 `app_icon.icns`，Windows `Crawler4j.exe` 现也改为绑定同源导出的 `app_icon.ico`，不再继续落回旧 Windows 图标。
- 最新修复：2026-04-22 已把正式发布输出目录清理行为补齐到 release 链路：`package-windows-release` 与 `package-macos-internal-release` 现在都会在生成新发布物前先清空 `dist/updates/<platform>/`，因此本地打包和打包后上传都不再继承旧 release 残留文件。
- 最新修复：2026-04-22 已把桌面更新服务器目录口径收口为 `mac/` 与 `win/` 并列：`deploy-macos-internal-release` 现在会把 `packages/crawler4j/dist/updates/macos/` 同步到 `CRAWLER4J_UPDATE_UPLOAD_TARGET/mac/`，同时新增 `uv run deploy-windows-release`，会通过 OpenSSH `sftp` 把 `packages/crawler4j/dist/updates/windows/` 同步到 `CRAWLER4J_UPDATE_UPLOAD_TARGET/win/`；`.env.example`、README、部署说明与打包配置回归已同步到新的 feed / 上传目录约定。
- 最新修复：2026-04-22 已为 Windows 正式交付补齐 `PyInstaller onedir + Velopack` 发布与宿主自更新主链：workspace 根新增 `uv run package-windows-release`，会在现有 onedir bundle 基线上写入 `crawler4j.update.json` 并继续生成 `Setup.exe` / `.nupkg` / `releases.win.json`；宿主系统层新增 `src.core.system.velopack`，`UpdateService` 已收口为 Sparkle / Velopack 平台分派，`src.ui.app:main` 也会在 GUI 初始化前先跑 Windows Velopack bootstrap。当前直接锁定该路径的是 `test_packaging_config.py`、`test_update_service.py` 与 `test_app.py`。
- 最新修复：2026-04-22 已继续收口系统设置里的 `关于` 页；该旧入口后续已由 2026-04-28 左侧一级 `关于` 页替代，`AboutDialog` 现在仅保留版本信息与项目链接，宿主更新入口在一级 `关于` 页。
- 最新修复：2026-04-22 已继续打通 macOS Sparkle 内部分发的发布侧签名链：`package-macos-internal-release` 现会在注入 `Sparkle.framework` 并改写 `Info.plist` 后自动对宿主 bundle 执行 ad-hoc `codesign`，避免 `generate_appcast` 再把更新归档判成 `No usable archives found`；同时发布脚本已支持通过 `CRAWLER4J_SPARKLE_KEYCHAIN_ACCOUNT`、`CRAWLER4J_SPARKLE_PRIVATE_ED_KEY_FILE` 或 `CRAWLER4J_SPARKLE_PRIVATE_ED_KEY` 显式提供 EdDSA 私钥来源。当前本机真实复验已确认：`codesign --verify --deep --strict` 通过，后续阻塞已前移为“若仍缺少私钥则 `generate_appcast` 会报 `Could not sign ... due to lack of private EdDSA key`”。
- 最新修复：2026-04-22 已把 macOS 内部 Sparkle unsigned 分发的签名口径固化到打包脚本：`package-macos-internal-release` 现会向 `Info.plist` 写入 `SUEnableCodeSigningValidation=false` 与空 `SUPackageSigningCertificate`，关闭宿主 app 的苹果代码签名校验，但继续保留 `SUPublicEDKey` 对更新包的 EdDSA 校验，适配未做 Developer ID / notarization 的内部发布场景。
- 最新修复：2026-04-22 已把 macOS 内部 Sparkle 发布 DMG 从“纯文件容器”升级为固定 Finder 图标视图：脚本现先生成可写 staging DMG、挂载后用 `osascript + Finder` 摆放 `Crawler4j.app` 与 `Applications`，再通过 `makehybrid -hfs-openfolder` 生成带自动打开目录标记的最终镜像并压缩为发布 DMG；当前直接锁定该布局链的是 `test_packaging_config.py` 的 DMG 命令序列与挂载输出解析回归。
- 最新修复：2026-04-22 已继续修正 macOS 内部 DMG 布局链的三处实机问题：Apple Silicon 上不再调用会失败的 `bless --openfolder`，Finder 布局脚本改为使用 `hdiutil attach` 返回的实际挂载卷名，且 `makehybrid` 现显式指定最终 HFS 卷名为 `Crawler4j`，避免本机已挂载同名卷时打偏布局或把中间目录名暴露给用户；最新 real artifact 已通过本机 `hdiutil attach` 验证。
- 最新修复：2026-04-22 已把 macOS 内部发布脚本的环境变量入口补成“默认读取 workspace 根 `.env`，也支持显式 `--env-file` 覆盖”，并新增 `.env.example` 模板；当前 `deploy-macos-internal-release` 仍旧通过 `rsync -av packages/crawler4j/dist/updates/macos/ -> $CRAWLER4J_UPDATE_UPLOAD_TARGET/` 上传，不会在 `generate_appcast` 失败后继续执行上传。
- 文档：2026-04-22 已为 macOS Sparkle 内部分发补齐服务器侧部署口径：当前推荐使用 `updates.<主域名>` 承载 `https://updates.example.com/crawler4j/appcast.xml`，仓库新增 `deploy/nginx/crawler4j-updates.example.conf` 样板，并在 `deployment-guide.md` 说明 Certbot/Let's Encrypt 与 `/srv/nginx/updates/crawler4j/` 静态目录约定；后续 `deploy-macos-internal-release` 的 rsync 目标与 nginx 目录现有统一口径。
- 文档：2026-04-22 曾把模块 UI 重构方案沉淀为 `module-hosted-ui-framework.md`，当时 UI 契约为 `ui_extension.pages[] + ui.declare_page`。当前 0.4.0 已被 `@page(...)` 注解模式取代，模块仍不得导出 `PyQt6` 页面。
- 最新修复：2026-04-21 已为 macOS 小范围内部发布补齐 Sparkle 线路的最小实现：宿主新增 macOS 打包版 Sparkle 运行时桥接，`system.auto_update` 现可同步控制 Sparkle 自动检查开关；workspace 根新增 `uv run package-macos-internal-release`，会在保留现有 `PyInstaller -> Crawler4j.app` 基线的前提下，把 `Sparkle.framework`、`SUFeedURL` / `SUPublicEDKey` 注入 bundle，并在 `packages/crawler4j/dist/updates/macos/` 生成内部 DMG / `appcast.xml` 发布脚手架。当前直接锁定该路径的是 `test_update_service.py` 与 `test_packaging_config.py` 的 Sparkle 回归。
- 最新修复：2026-04-21 已对齐 VirtualBrowser `addBrowser` 的代理协议口径：宿主创建环境时会把代理 `protocol` 统一转成官方文档示例所用的大写 `HTTP/HTTPS/SOCKS5`，避免继续把 IP 池生成的 `http://user:pass@host:port` 代理以小写协议透传给外部浏览器；同时 `VirtualBrowserClient.add_browser()` 在 `addBrowser` 返回非 2xx 或 `success=false` 时，现会把状态码与响应正文一起写入主日志并抛出带正文的异常，后续排查代理认证失败或外部宿主 500 不再只剩一条裸 `HTTPStatusError`。对应回归当前锁定于 `test_virtualbrowser_client.py` 的协议归一化与 `500` 正文透传断言。
- 最新修复：2026-04-21 曾把 ATM `ExecutionRunner` 的 `on_cleanup` 默认最大执行时间从 `8s` 提高到 `120s`；后续 2026-04-28 已改为配置中心可配，当前 hook 预算已被 2026-04-30 删除，只保留环境回收超时。
- 文档：2026-04-21 已修正根 `docs/index.md` 中 `ctrip-real-site-e2e-closeout.md` 导航项的非法 YAML 标题写法，并把测试收口页标题与 `document-index.md` 入口统一收口为普通字符串；当前 `docs-stratego` 不应再因该条 `title` 解析失败而阻塞源仓同步。
- 最新修复：2026-04-21 已继续收口桌面宿主的 `qasync`/Qt 定时器崩溃链：`src.ui.app:main` 现先对 `qasync` 安装宿主侧 `_SimpleTimer` 兼容补丁，再以单次 `run_until_complete(_run_application(...))` 驱动完整 UI 生命周期，避免在 `PyQt6 6.10.1 + qasync 0.28.0` 组合下继续走 `QObject.startTimer()` 私有路径并多次启停 `QApplication.exec()`；统一日志服务的 `LogSignals(QObject)` 也改为延迟创建，减少 `QApplication` 之前提前实例化全局 `QObject` 的风险。当前直接锁定这条兼容层的是 `test_qasync_compat.py`，UI 启动与日志台相关回归已补跑 `test_app.py` / `test_log_console.py` / `test_dashboard.py` / `test_shell.py`。
- 最新修复：2026-04-21 已为 ATM `ExecutionRunner` 的终态收尾补上超时防线：历史上 `on_success` / `on_failure` / `on_timeout` / `on_cleanup` 与环境回收曾有宿主级 timeout guard；2026-04-30 后 hook 预算已删除，只保留环境回收 timeout guard；2026-06-09 后对象 `cleanup(ctx, outcome)` 也不再设置宿主固定执行超时，避免模块业务收尾被短预算截断。对应回归当前锁定于 MMS 对象生命周期与 ATM 环境回收路径。
- 最新修复：2026-04-21 已补齐 MMS 正式模块安装/升级的原子切换防线：ZIP 覆盖安装与本地目录安装现统一走“先在 staging 目录做 manifest 校验 + `__init__.py` 导入预检，再切换到目标目录；若切换后任一步失败则删除坏包并回滚旧版本”的路径，避免坏包在升级失败时覆盖掉可用模块；对应回归已补到 `test_external_module_install.py`，当前锁定 ZIP 升级失败回滚、本地目录安装失败回滚与坏包导入预检三条路径。
- 最新验证：2026-04-22 已对“Windows 打包版启动竞态 + Velopack 图标链路”补跑最小必要回归：`uv run pytest packages/crawler4j/tests/unit/test_ui/test_app.py -q`（`5 passed`）、`uv run pytest packages/crawler4j/tests/unit/test_sdk/test_packaging_config.py -q -k "test_windows_release_write_update_config_to_bundle or test_windows_release_build_vpk_pack_command_uses_global_vpk or test_windows_release_build_vpk_pack_command_uses_dnx_when_requested or test_windows_release_build_vpk_pack_command_wraps_batch_shims_with_cmd or test_windows_release_build_vpk_pack_command_uses_normalized_dnx_version"`（`5 passed, 51 deselected`），以及目标文件 `uv run ruff check ...` 通过；同次尝试执行整份 `test_packaging_config.py` 时仍命中一个与本次改动无关的既有断言失败：`test_root_app_package_does_not_reexport_sdk_cli_command` 仍在校验 `crawler4j-contracts` 依赖上限。
- 最新验证：2026-04-21 已再次按“多子 agent review + 主线程修复/复验”执行 fresh gate；当前主线程结果为 `uv run pytest -q`（`523 passed`）、`uv run ruff check .`、强化后的 `uv run python scripts/smoke_test_ui.py` 与 `uv run build` 全部通过。2026-04-22 又补跑了 Windows Velopack 定向回归：`uv run pytest packages/crawler4j/tests/unit/test_core/test_system/test_update_service.py packages/crawler4j/tests/unit/test_ui/test_app.py packages/crawler4j/tests/unit/test_sdk/test_packaging_config.py -q`（`50 passed`）与目标文件 `uv run ruff check ...`。当前正式交付结论仍为 `No-Go`，剩余外部阻塞已收口为 `ctrip` 真站 E2E 留证、远端 release/交付批次刷新，以及 Windows 真机签名/安装/升级留证。
- 最新修复：2026-04-21 已补齐宿主 `qasync` UI 重入防线：`EnvListWidget` 的创建/编辑/销毁链路不再在协程内调用阻塞式 `dialog.exec()` 或静态 `QMessageBox.*`，统一改为 `open()` + await 完成信号；`DashboardPage` 的定时刷新也已改为“新一轮开始前先取消上一轮 pending load”的串行刷新模型，避免在 REM 环境操作后的模态提示窗口内再叠加仪表盘刷新任务，把主线程推入 `Cannot enter into task ... while another task ... is being executed` 并进一步诱发 Qt 定时器崩溃。对应回归当前锁定于 `test_env_list_widget.py` 与 `test_dashboard.py`。
- 最新修复：2026-04-23 已完全移除 `data.db.module_dataset_manifests`。`managed_dataset` 现在只保留 `data.db.module_datasets` 作为记录事实源；宿主启动会自动 `DROP TABLE IF EXISTS module_dataset_manifests` 清理旧表，`replace_resource_records()` 对非空重写仍沿用现有行的最早 `created_at`，但写空资源记录后不再保留额外的 dataset 级元数据行。当前直接锁定这次收口的是 `test_module_data_store.py`，`test_runtime_capabilities.py` 与 `test_module_data_table_page.py` 只覆盖工具面 / UI 兼容语义。
- 最新验证：2026-04-20 已按“4 个专业 code review 子 agent + 主线程修复/复验”完成全仓严苛复核；当前主线程 fresh gate 结果为 `uv run pytest -q`（`485 passed`）、`uv run ruff check .`、`uv run python scripts/smoke_test_ui.py`、`uv run build` 与 macOS 桌面打包全部通过；当前正式桌面打包口径已收敛为 `uv run package-desktop`，发布产物固定落到 `packages/crawler4j/dist/desktop/<platform>/`，PyInstaller 中间构建目录固定落到 `packages/crawler4j/build/pyinstaller/<platform>/`；`ctrip_crawler` 当前源码 `check full`、模块仓 `uv run pytest -q`（`193 passed`）、fresh ZIP `/tmp/ctrip_crawler-acceptance.zip` 的 `host install preview --skip-remote-check` 与宿主 `host devlink list` 活跃状态继续沿用 `2026-04-19` 留证。
- 最新修复：2026-04-20 已把 ATM Service Job 的 5 秒兜底巡检从 `APScheduler` 周期 job / `run_coroutine_job()` 包装收口为 `JobController` 挂在主 async loop 上的后台协程循环；当前批次 Cron 仍走 `APScheduler`，Service Job 巡检仍与主 `qasync`/asyncio loop 共享同一事件循环，只是不再复用 `APScheduler` 的协程 job 包装。启动期会先做 bootstrap 调和，作业激活/更新也会定向 `reconcile_job()`，后续 5 秒 periodic loop 只作兜底；单轮巡检补上超时取消与收尾恢复，避免 UI 环境操作或外部运行时超时后把巡检卡成永久 pending、持续刷 `maximum number of running instances reached (1)` warning；对应 ATM 单测与设计/用户文档已同步更新。
- 最新修复：2026-04-20 已为桌面打包补齐 `sinanz` 单文件模块依赖的共享 `resources/` 目录收集，并在 Core 验证码能力里显式向滑块/点选求解器传入 bundle 内 `asset_root`；当前打包版不应再因缺失 `slider_gap_locator.onnx` 等内嵌模型资源而回退本地处理。
- 最新修复：2026-04-20 已修正 Core 滑块验证码对 `sinanz` 的 `asset_root` 传参层级；滑块链路现在显式传入 `resources/models/`，不再把共享资源根 `resources/` 误当作模型目录，避免打包版继续查找错误路径 `Contents/Resources/resources/slider_gap_locator.onnx`。
- 发布：2026-04-22 `crawler4j-sdk 0.4.0` 已完成本地 SDK 回归（`uv run pytest packages/crawler4j/tests/unit/test_sdk/test_cli_scaffold.py packages/crawler4j/tests/unit/test_sdk/test_packaging_config.py -q`，`101 passed`）、wheel/sdist 构建，并通过 `uv run publish crawler4j-sdk` 发布到 PyPI。
- 最新结论：2026-04-20 `v0.2.0` 已完成正式 Git tag、GitHub release 与 macOS 发布资产上传；2026-04-22 又补齐了 Windows `PyInstaller onedir + Velopack` 发布链。当前仍保留的交付边界已收口为 `ctrip` 本轮 DevLink + ZIP 双链真实站点 E2E 留证，以及 Windows 真机签名/安装/升级证据缺口。
- 最新修复：2026-04-20 已移除 REM 环境列表页里 `QThread + 共享 asyncio loop` 的旧执行模型；环境 `start/stop/pause/resume/create/destroy` 现统一切到 UI 主 `qasync` 事件循环里的串行异步任务，并在操作期间禁用表格、创建和刷新入口，避免连续点击时触发跨线程 loop 复用或并发重入。
- 最新修复：2026-04-20 已修正 `crawler4j-sdk page create` 的独立模块仓脚手架：生成的代码型页面现在在缺少 `PyQt6` 时仍可被 `crawler4j check full` 安全导入，只在真正实例化页面时才提示安装宿主 GUI 依赖；同时把 SDK / Contracts / Root app 的 `0.x` 兼容区间从宽泛的 `<1.0.0` 收紧到当前 minor 的 patch 版本，避免未来 `0.x` breaking minor 被 fresh install / `uv sync` 静默拉入。
- 最新修复：2026-04-20 已把默认环境名占位的 `SELECT max + INSERT` 收回到同一个 SQLite `BEGIN IMMEDIATE` 写事务里；环境名计算与 `CREATING` 占位记录持久化现同步完成，不再依赖“查完再用 `pool.add()` 另开事务写入”的非原子路径，避免并发创建时重复发放 `env-YYYYMMDD-N` 名称。
- 最新修复：2026-04-20 已按固定环境池正式业务口径收紧调度与租约语义：当前只从 `eligible=true + READY + 无租约` 环境发号，`KEEP_ALIVE` 留下的 `RUNNING` 环境不会自动回池；若候选环境在 `get_env` / 租约阶段被其他任务先抢走，或在发号快照后被资源池卡片改成不可发号，任务会回到等待席位并保留原 `waiting_since`，不再直接记为失败；对应 ATM/REM 单测与开发/设计/测试文档已同步更新。
- 最新修复：2026-04-20 已把统一日志服务对 `APScheduler` 的最低输出级别收口到 `WARNING`；当前第三方调度器的周期心跳不再持续刷屏，而 ATM `Service Job` 的 5 秒兜底调和也已改为控制器挂在主 async loop 上的后台循环，不再依赖 `APScheduler` 的 `Running job` / `executed successfully` 日志；对应日志单测与用户设置文档已同步更新。
- 最新修复：2026-04-20 已修正 ATM `任务调试` 对话框生成 VS Code `launch.json` 时固定沿用表单端口的问题；当前若调试服务为避让占用端口而改写 attach 地址，对话框会优先写入活动调试会话的真实 `attach_host/attach_port`，失败或已结束会话则回退到表单请求值；对应 Qt 单测与开发者调试文档已同步更新。
- 最新修复：2026-04-20 已修正 ATM `任务调试` 对话框日志框的轮询刷新行为；当前当日志内容未变化时不会重复整块重绘，用户手动拖到中间查看旧日志时滚动位置会保留，停留在底部时则继续自动跟随最新输出；对应 Qt 单测与开发者调试文档已同步更新。
- 最新修复：2026-04-20 已修正 ATM `任务调试` 对话框的“重新开始”语义；当前点击 `重新开始` 时会先停止旧调试会话，再按当前表单里的端口、`等待 IDE 附加`、`启动后立即断住`、运行态参数等值新建并启动调试会话，不再沿用旧 session 的隐藏配置；对应 Qt 单测、调试集成回归与开发者调试文档已同步更新。
- 最新修复：2026-04-20 已把 debug worker 的模块刷新时机后移到 attach 完成之后；当前勾选 `等待 IDE 附加` 时，宿主会先等待调试器附加，再执行 `ModuleRegistry.refresh()` 并进入 `ExecutionRunner`，避免 `env_selector`、workflow 与 task 等早期模块代码在 debugger 就绪前就被导入/跑过；对应调试集成回归与开发者调试文档已同步更新。
- 最新修复：2026-04-20 已为 ATM `任务调试` 的 VS Code attach 配置补上显式 `pathMappings`；当前 `Attach to Crawler4j` 会把 `${workspaceFolder}` 映射到宿主实际加载的 DevLink 模块真实目录，减少因工作区别名/符号链接/路径漂移导致的“Attach 成功但断点不生效”；对应 VS Code 配置单测与开发者调试文档已同步更新。
- 最新修复：2026-04-20 已把 debug worker 的 attach 阻塞从轮询 `debugpy.is_client_connected()` 收紧为官方 `debugpy.wait_for_client()` 路径；当前勾选 `等待 IDE 附加` 时，未完成 IDE attach 前不应再提前进入 `running`；对应调试集成回归已把 `stop_on_entry=false` 默认分支补上“attach 前保持 waiting”断言。
- 最新修复：2026-04-20 已把仓库根目录 `.vscode/launch.json` 里的 `Crawler4j: Start App` 显式收紧为 `subProcess=false`，并在开发者调试文档补充“不要把主程序 VS Code launch 调试和任务 worker attach 调试混用”的说明，降低主程序调试链干扰 task debug attach 的风险。
- 最新修复：2026-04-20 已把桌面打包态的 task debug worker 启动链显式分叉；源码运行继续走 `python -m src.core.debug.worker_entry`，PyInstaller 打包态则改为宿主内嵌 `--crawler4j-debug-worker` 入口，避免 `sys.executable` 指向 `Crawler4j.app` 时再次拉起一份 GUI 主窗口；对应 UI 单测、debug service 单测与调试文档已同步更新。
- 最新修复：2026-04-20 已把 `debugpy` 及其 `_vendored` 数据文件、vendored `pydevd` 的 `.py` 运行时模块一并显式纳入 PyInstaller 桌面包；当前打包版 task debug worker 不再因 bundle 漏收 `debugpy/_vendored` 或 `_pydevd_bundle.pydevd_constants` 这类 vendored 模块而直接以退出码 `1` 失败；对应打包配置单测、开发者调试文档与桌面重打包 smoke 验证已同步更新。
- 最新修复：2026-04-20 已继续把 frozen 场景下 `debugpy.listen()` 的 adapter 启动链收口到宿主内嵌 `--crawler4j-debugpy-adapter` 入口，并在 worker 启动前通过临时 launcher 脚本显式 `debugpy.configure(python=...)`；当前真包 smoke 已验证 macOS 桌面包可进入 `waiting_for_attach` 且端口真实监听，不再回退到 GUI 主程序。
- 最新修复：2026-04-20 已把 ATM `新建作业` 弹窗中的 `配置运行模板`、`取消`、`创建/保存` 按钮切到共享 `StyledButton` 组件，并统一为 `40px` 高度；同时把“运行配置”预览说明改为按当前宽度自动回流并同步最小高度，避免长文本在真实窗口里向下溢出、压到下面按钮区域；对应 Qt 单测已补“使用公共组件”和“预览文本与按钮保持间距”的断言。
- 最新修复：2026-04-20 已把 `docs-stratego` 通知 workflow 的自动监听分支从 `feature/task-plugin-system` 收口到 `main`，并在发送 `repository_dispatch` 前先去掉 `DOCS_STRATEGO_DISPATCH_TOKEN` 中意外带入的 `CR/LF`，避免 `Authorization` header 被换行拆断；对应部署说明、运维索引、文档索引与 workflow 静态回归测试已同步更新。
- 最新修复：2026-04-22 已为 macOS 内部 Sparkle 分发补齐“安装 Sparkle + 打包并上传 nginx 目录”闭环：workspace 根新增 `uv run install-sparkle --archive ...` 与 `uv run deploy-macos-internal-release`，前者可把 Sparkle release archive 安装到 `packages/crawler4j/vendor/macos/sparkle/`，后者复用 `package-macos-internal-release` 生成 DMG / `appcast.xml` 后再通过 `rsync -av` 上传到 `CRAWLER4J_UPDATE_UPLOAD_TARGET`；README、部署/交付文档、vendor 说明与打包配置回归测试已同步更新。
- 设计：2026-04-19 ATM 固定环境池方案已按“模块资源池资格卡片 + 等待队列 + FIFO 补位 + 资源池卡片更新事件 + 轻量定时调和”的口径落地到宿主和 SDK V1。
- 规划：2026-04-19 `REQ-009` / `CR-009` / `TASK-023` / `API-007` 已完成等待队列、FIFO 补位、资源池隔离与等待席位自动超时收口的本地实现与单测验证；当前待收口到 PR，并补真实业务模块接入验证。
- 任务：TASK-011-mms-settings-store-and-module-state-persistence、TASK-012-mms-trust-gate-and-custom-ui-loading、TASK-013-stabilize-module-root-entry-shim-and-sdk-assembler、TASK-021-add-signal-driven-confirmation-panel、TASK-022-add-module-audit-event-storage-tools、TASK-023-add-atm-fixed-pool-service-queue
- 变更：CR-001-version-and-release-governance-alignment、CR-002-quality-gate-and-docs-navigation-alignment、CR-003-mms-settings-and-ui-extension-compliance、CR-004-atm-signal-driven-confirmation-ui、CR-005-devlink-run-once-reload、CR-008-module-audit-event-storage-split、CR-009-atm-fixed-pool-service-queue
- 文档：2026-04-17 已把 `docs/03-developer-guide/` 统一收口到当前 `crawler4j-sdk` 命令树与模块契约，正式口径不再固定 `crawler4j-sdk==某个版本`；首页、quickstart、CLI 参考、调试、交付与排障现统一以 `module/task/workflow/page/data-table/env-selector/config/package/release/host/check` 分组命令和最新帮助输出为准，旧迁移页已删除。
- 文档：2026-04-19 已对固定环境池 / 环境队列开发者文档完成“1 个专业文档 reviewer + 2 个模块开发者 reviewer”的三轮苛刻复核；`docs/03-developer-guide/index.md`、`reference-core-capabilities.md`、`build-modules.md`、`debugging.md`、`troubleshooting.md` 与 `API-007` / `atm-resource-pool-queue-design.md` 现统一钉死 `Service Job` 前提、`resource_pool / selector_name / wait_timeout` 语义、`env_id` 来源、`@env_selector(...)` 入口、`replace_resource_pool_snapshot(...)` 全量重建与等待状态文案分层，最终 3 个 reviewer 全部给出 `无 blocker`。
- 文档：2026-04-22 已把 SDK 版本独立提升到 `0.4.0`，并同步仓库根与子包 `README.md`、开发者指南、`release-notes.md`、`version-governance.md`、`.factory/project.json` 及版本摘要；当前源码版本线为 `crawler4j 0.2.0 / crawler4j-sdk 0.4.0 / crawler4j-contracts 0.2.0`，同时保留“最近正式 Git tag 仍为 `v0.1.1`”的分层描述。
- 文档：2026-04-17 已根据“小白模块开发者”苛刻审稿完成第二轮修订：补齐了 `--repo` 占位值说明、`module set default-workflow`、`ui:DashboardPage` / `ui/__init__.py` 导出关系、`TaskResult.data` / `run_subtask()` 真实语义、`host install` 与宿主 UI 两条互斥安装路径、DevLink/ATM 最短调试判据、`core:data_table` 最短分叉排障，以及 `reference-sdk-and-cli.md` 的完整可复制命令前缀。
- 文档：2026-04-17 已完成 6 个“小白模块开发者”子 agent 二轮复核，分别覆盖上手、命令复制、调试、交付四类场景，最终全部给出 PASS；当前 `docs/03-developer-guide/` 可直接作为标准模块开发主手册使用。
- 文档：2026-04-25 已按真实实现把模块审计能力补回开发者指南主链路：`reference-core-capabilities.md`、`api-design.md` 与 `module-config-runtime-data-contract.md` 现统一明确 `ctx.db.audit(...).append/query`、`data.db.module_audit_events` 以及 `module_datasets` 只承载快照型高价值数据的边界。
- 文档：2026-04-25 已继续收口审计事件示例口径：开发者指南与设计契约现统一要求把 `previous_status` / `next_status` / `result` / `reason` 作为顶层审计字段写入，`payload` 只保留模块私有扩展字段；`ctx.db.audit(...).query` 章节也已补记当前返回事件结构。
- 文档：2026-04-17 `docs/02-user-guide/` 已完成第二次完全重写并按真实普通用户路径重新排序为 `安装与第一次打开 -> 首次设置 -> 开始使用 -> 日常使用 -> 管理员指南`；本轮新增“新手只看 3 个入口”“参数来源表”“运行模板入口固定说明”“结果只认一个入口”“作业状态下一步动作”“IP 池最短闭环”“可复制报障模板”，根 `docs/index.md` 导航顺序已同步调整。
- 文档：2026-04-17 `docs/01-getting-started/` 已进一步压缩为单页模式，当前只保留 `了解 crawler4j` 这一篇作为正式入口；正文改为面向客户的产品介绍，不再拆成多页教读者选路径，原辅助页已删除。
- 文档：2026-04-17 已新增 `docs/02-user-guide/exception-cases.md`，面向普通用户与现场支持按症状收敛“应用打不开、页面空白、模块未启用、执行一次没反应、任务实例全失败、环境不可用、升级失败、结果找不到”8 类异常；每个案例统一采用“现象 / 先看哪里 / 先做什么 / 什么情况升级给管理员或研发”结构，`docs/index.md`、`docs/02-user-guide/index.md`、`document-index.md` 与 `.factory/memory/doc-map.md` 已同步接入。
- 文档：2026-04-17 已新增 `docs/02-user-guide/job-detail-guide.md`，把“作业详情整图说明”单独收口为培训讲义页；正文固定按 `任务实例表 -> 结果/错误 -> 任务日志 -> 成功/失败判据 -> 不同状态下一步动作` 讲解，当前内容可直接用于培训。
- 文档：2026-04-17 已为桌面客户端新增 `📘 使用文档` 树形导航页；现已收口为“文档中心”结构，左侧最上面是直接打开 `01-getting-started/index.md` 的顶层文档 `开始前`，其余内容再按 `使用指南 / 开发指南` 两组展示；分组标题使用可见的文字箭头并支持整行点击展开/收起，右侧不再显示重复的标题/副标题/当前文档头部；文档页现已改为在资源加载阶段按正文可视宽度缩放本地截图，避免 `QTextBrowser` 被原始大图撑爆，并在文档加载后统一把 Markdown 链接改成高对比浅蓝加下划线，避免沿用 Qt 默认深蓝；开发态始终直接读取工作区 `docs/`，打包态读取构建时拷入包内的 docs 快照；当前 PyInstaller spec 已显式包含 `docs/index.md`、`docs/01-getting-started/`、`docs/02-user-guide/` 与 `docs/03-developer-guide/`，正式发布包可直接按 Markdown 展示上述文档，文内相对跳转与截图在客户端内可直接查看。
- 文档：2026-04-17 已按用户要求对当前 `docs/02-user-guide/` 执行“3 个专业产品文档子 agent 分工重写 + 6 个普通用户子 agent 两轮苛刻复核”闭环；普通用户首轮反馈为 `3 PASS / 3 FAIL`，集中指出入口顺序、参数来源、运行模板入口、状态决策和结果入口收束仍不够傻瓜，补齐后第二轮 6 个普通用户全部转为 `PASS`，并明确愿意把文档原样发给新同事。
- 文档：2026-04-17 已新增 `docs/04-project-development/06-testing-verification/ctrip-real-site-e2e-closeout.md`，把 `ctrip` 真实站点 E2E 的前置条件、DevLink/ZIP 双链路执行顺序、证据要求和放行条件收敛为单一正式入口；`test-plan.md`、`acceptance-checklist.md`、`document-index.md` 与 `.factory/memory/doc-map.md` 已同步接入。
- 维护资产：2026-04-18 仓库根 `scripts/` 现保留 `build_workspace_packages.py`、`db_cli.py` 与 `smoke_test_ui.py` 三个维护脚本；其中 `build_workspace_packages.py` 已扩成统一构建/发布包装器，并通过 workspace 根 `pyproject.toml` 暴露为 `uv run build` / `uv run publish` 短命令；构建时会在 workspace 根顺序执行 `crawler4j-sdk / crawler4j / crawler4j-contracts` 的 `uv build --clear` 并把产物分别落到各自包的 `dist/`，发布时则自动指向各自包的 `dist/*`；README、部署说明、维护指南与 `test_packaging_config.py` 已同步更新。
- 最新修复：2026-04-18 已把系统日志链路进一步收口为唯一日志服务：`src.core.foundation.logging.logger` 现统一承接 Core 直接日志、模块 `ctx.logger` 默认注入以及标准库 `logging` 记录；`配置中心 -> 资源` 修改 `日志级别` / `日志保留` 后会立即热更新根日志 handler 与文件日志，不再只在下次启动时生效；仪表盘与任务详情日志窗格也已切到直接订阅统一日志信号，系统日志与模块日志现走同一条实时链路。
- 最新修复：2026-04-25 已把模块持久数据契约进一步拆分为“快照数据 + 审计事件”两条正式能力面：宿主保留 `data.db.module_audit_events`，运行时通过 `ctx.db.audit(...).append/query` 独立追加和查询 append-only 事件历史；`module_datasets` 与 `ctx.db.into(...).replace` 继续保持快照语义，不再承担审计事件历史存储。
- 最新修复：2026-04-25 已把审计写入从旧 `ctx.tools.call("db.*")` 收口到 `ctx.db.audit(...)`；`declare_ui` 仍保持无副作用边界，SDK 检查态工具不暴露任何 `db.*` 工具。
- 最新验证：2026-04-18 审计事件能力与文档导航回归已通过质量门：全量 `uv run pytest -q` 为 `411 passed`，全量 `uv run ruff check .` 通过；其中审计事件直接命中的 `test_module_data_store.py`、`test_runtime_capabilities.py`、`test_data_capability.py` 共 `17 passed`，`test_settings_store.py` `9 passed`，`test_help_page.py` `4 passed`。
- 最新验证：2026-04-25 审计事件 API 收口与 `ctx.captured_data` 删除回归已通过定向质量门：`uv run pytest packages/crawler4j/tests/unit/test_core/test_atm/test_execution_runner.py packages/crawler4j/tests/unit/test_core/test_atm/test_runtime_capabilities.py packages/crawler4j/tests/unit/test_core/test_persistence/test_module_data_store.py packages/crawler4j/tests/unit/test_sdk/test_data_capability.py packages/crawler4j/tests/unit/test_sdk/test_cli_scaffold.py packages/crawler4j/tests/unit/test_sdk/test_taskcontext.py -q` => `149 passed`；目标文件 `uv run ruff check ...` 通过。
- 最新修复：2026-04-18 已把 `core:data_table.lock_key` 的语义正式收口为“Core 临时锁”：开发者文档与设计契约现明确禁止把 `lock_key` 当成业务占用态开关使用；Core 运行时和 `crawler4j-sdk check full` 现在都会拒绝“`lock_key` + `occupied`/`occupied_label` 业务占用列”同时声明，避免模块作者再次配出重复“占用中”列。
- 最新修复：2026-04-17 已把 `playwright_local` 从“环境创建成功但无 `page` 可用”修到真实站点可执行：Provider 现在会真实拉起本地 Playwright 浏览器，优先尝试 bundled Chromium，再回退 system Chrome；对应 `test_playwright_provider.py` 已锁定 open/connect/close/reset/health_check 语义。真实 `ctrip` 回放已能进入登录页，业务失败也会正确落到任务失败而不是 workflow 假绿。
- 最新修复：2026-04-17 已继续收口 `VirtualBrowser` 观测与恢复链：`VirtualBrowserClient.launch_browser()` 现会把可恢复的 `launchBrowser` 错误写入主日志，并把 `browser is running` 冲突纳入 stop-then-retry；新增回归测试覆盖 `500 DevTools port` 与 `400 is running` 两类恢复路径。通过直接调用 `localhost:9002` 的 `addBrowser -> launchBrowser`、冷启动外部应用、复用旧 worker 和 `chrome_version=139..146` 扫描，已确认当前剩余阻塞收敛为外部 VirtualBrowser 运行时故障，不再是宿主 REM 配置或 provider 默认值问题。
- 历史记录：2026-04-16 曾把 ATM 与 `core:data_table` 构造的 `TaskContext.logger` 接到统一应用日志器，覆盖当时的模块生命周期入口、workflow 与 task script 日志；同日还修复任务监控手动批次 `执行一次`、环境收尾、`TaskSignal.wait_for_confirmation`、数据列表滚动条、发现期导入错误可见性和 DevLink reload 语义。当前 0.4.0 已删除 `before_run` 等 lifecycle hook，正式入口以 workflow 主体和 page action 为准。
- 最新修复：2026-04-17 已继续细化 ATM 手动批次“执行一次”反馈：当任务仍停留在 `PENDING` 的环境创建/浏览器启动阶段时，任务监控列表现在会显示 `环境启动中` 与顶部启动条，等环境真正可用后自动切回 `执行中`；同时 `VirtualBrowserProvider.open()` 不再吞掉 `launchBrowser` 细节，顶部 Toast 会直接展示 `VirtualBrowser launchBrowser 失败: ...` 这类具体错误，减少“按钮像卡死”的误判。
- 最新修复：2026-04-19 已补上 VirtualBrowser 选中已运行环境的 `ws_url` 自愈：`VirtualBrowserProvider.connect()` 在 `BrowserHandle.ws_url` 为空时会先从浏览器详情回填可连接入口，再进入 `safe_connect()`，避免 `select` 模式下 `READY -> BUSY` 的已开窗口环境直接报 `Missing ws_url for browser 1`；对应 provider 回归测试已补齐。
- 最新修复：2026-04-19 已继续增强 VirtualBrowser 运行时详情恢复链：`get_browser_detail()` 支持字符串/嵌套 ID，`get_browser_running_detail()` / `get_browser_runtime_detail()` 会合并运行列表作为兜底；`_extract_cdp_endpoint()` 也补齐了 `browserWSEndpoint` 等真实字段和递归抽取，避免 live API 结构差异继续卡住连接。
- 最新修复：2026-04-19 已按 live probe 结果调整 VirtualBrowser 运行态优先级：`get_browser_runtime_detail()` 现在以 `getBrowserRunningList` 为第一来源，只有运行列表没有记录时才退回 `getBrowserList`；连接恢复直接使用运行态 `debuggingPort`，与真实 API 结构对齐。
- 最新修复：2026-04-19 已修正模块管理页的 `qasync` UI 重入问题：`ModuleListWidget` 在异步安装 / DevLink / 升级 / 检查更新链路里不再直接调用阻塞式 `dialog.exec()` 或静态 `QMessageBox.*`，统一改为 `open()` + await 完成信号；对应单测已锁定 DevLink 添加成功与通用对话框两条非阻塞路径。
- 最新修复：2026-04-19 已修正 `EnvironmentManager.start_env()` 对 `BUSY` 的默认判断：先问 provider 窗口是否真的已开，已关窗的复用环境会先 `open()` 再 `connect()`，避免 `select` + lease 场景把 `READY` 复用环境误当成“已开窗待连接”。
- 最新修复：2026-04-19 已为 VirtualBrowser 运行态回填加短重试：`VirtualBrowserProvider.connect()` 在 `ws_url` 为空时会对 `get_browser_runtime_detail()` 最多重试 3 次再失败，避免真实站点运行态信息尚未准备好就过早进入 `safe_connect()`。
- 最新修复：2026-06-06 已把指纹浏览器 provider 生命周期并发风险从 VirtualBrowser 启动链扩展收口到 VirtualBrowser / BitBrowser 的关闭、销毁、重置、状态检查、来源列表与配置更新。两个 provider 现统一用生命周期锁包住外部管理 API 和 Playwright/CDP 生命周期入口，内部等待链改用 unlocked helper 避免锁内递归；当 handle 丢失但 `external_id` 仍在时，销毁仍会删除真实外部环境。定向回归 `test_provider.py` + `test_bitbrowser_provider.py` + `test_virtualbrowser_client.py` + `test_destroy_env.py` + `test_post_create_actions.py` 为 `53 passed`，目标文件 `ruff check` 通过。
- 最新修复：2026-04-18 已把 ATM 手动批次“执行一次”的中止链路真正收口：当作业处于 `环境启动中` / `执行中` 时，列表主按钮会切成 `⏹ 中止`，弹窗支持中止运行；此前运行中任务只会记录 stop request、无法打断模块协程的问题已修复，`ExecutionRunner` 现在会主动 cancel 运行中的模块协程，`TaskContext.wait()` / `run_subtask()` 在 stop 后也会尽快抛 `asyncio.CancelledError`。2026-04-30 已移除 `on_cleanup` 运行链，后续已进一步删除模块侧信号/环境处置入口，对象生命周期走可选 `setup(ctx, workflow)` 与 `cleanup(ctx, outcome)`，环境由宿主回收。
- 最新修复：2026-04-16 已删除脚手架与客户端内残留的声明式 `ui/config_schema.json` / `strategy.yaml` 链路；`module.yaml` 继续作为唯一模块清单，默认配置页改为只读取宿主持久化的模块设置；后续 2026-04-22 又继续把页面脚手架正式收敛为 hosted page V1，不再生成 `micro_app` 页面。
- 历史记录：2026-04-16 曾把模块持久配置从旧 `configs` 聚合键迁到 `config.db.module_config_entries`，并把部分 ATM / Debug 临时输入改走 `ctx.runtime`。该中间口径已被 2026-05-01 的 0.4.0 审查修复取代：`execution.params`、`job.params` 合并注入和 `ctx.runtime["params"]` 不再属于正式运行面。
- 最新修复：2026-04-17 已把真实生效的模块详情页补回 `配置` 标签，宿主现在直接提供模块级配置与工作流级覆盖的 YAML 编辑器；2026-04-26 已进一步切换为 QScintilla YAML 编辑器，并把 YAML 格式校验、顶层映射对象校验与重复键校验收口到独立验证层，标准 YAML flow mapping 保存后会规范化回块格式展示。`core:data_table` 的 schema 与 dataset records 当前只认 `data.db`，运行时代码不包含旧 `state.db.kv_store` 自动迁移逻辑；曾在 2026-04-22 把模块详情页入口统一切到 `ui_extension.pages[]`，该入口后续已被 0.4.0 `@page(...)` 注解模式取代。
- 最新修复：2026-04-17 已重排模块详情页 `配置` 标签布局：移除了模块配置与 Workflow 配置外层卡片框，页面主体改为可拖拽的纵向分隔布局，默认按“模块配置约 70% / Workflow 配置约 30%”分配高度；用户可直接拖动中间分隔条手动调整两个 YAML 编辑区的可视高度，长模块配置不再被底部区域挤压得过于局促；2026-04-26 起两个编辑区由 QScintilla 提供行号、折叠、YAML lexer 与校验错误行标记。
- 最新修复：2026-04-17 已把 `core:data_table` 记录编辑弹窗里的文本输入框从局部 `QLineEdit` 收口到公共 `src.ui.components.line_edit.StyledLineEdit`，并删除该弹窗对 `QLineEdit` 的重复局部样式覆盖；对应 UI 单测现同时锁定公共下拉框与公共单行输入框约束，避免后续表单弹窗再次回退到原生组件。
- 最新修复：2026-04-17 已为 `module.yaml` 增加 `config_defaults` 契约；宿主首次加载模块时会按 `config_defaults.module` / `config_defaults.workflows` 初始化 `config.db.module_config_entries` 一次并写入初始化标记，后续升级与刷新不再自动覆盖；模块详情页新增“恢复模块默认 / 恢复 Workflow 默认”按钮，并在警告确认后按当前 manifest 模板重写对应 scope。
- 最新修复：2026-04-17 已补齐 `docs/04-project-development/04-design/module-config-runtime-data-contract.md`，并把开发者指南中的 `ctx.config`、`ctx.runtime`、`ctx.state`、`db.*` 使用边界同步为统一规范，供模块开发者按同一契约开发。
- 最新修复：2026-04-17 已把模块分发契约收敛到 `module.yaml.upgrade_source`；正式模块安装入口现支持 `本地 ZIP` 与 `GitHub 源 URL` 双模式，安装前会校验 GitHub 仓库是否存在；DevLink 注册同样执行清单与升级源预检；模块管理页新增 `检查更新` 与行级 `升级` 按钮，正式模块可按 GitHub Release 自动下载并执行原子升级，运行中任务会阻断升级。
- 最新修复：2026-04-17 已开始重构 `crawler4j-sdk` CLI V1，旧平铺命令树已被 `module / task / workflow / page / env-selector / config / package / release / host / check` 分组体系替换；脚手架不再创建 `data/` 抽象层，后续 2026-04-22 又进一步把 `page create` 与 `check full` 全部切到 hosted page V1，不再维护 `detail_menu`、`entry` 或 `ui:PageClass` 旧契约。
- 最新修复：2026-04-17 已补齐 `crawler4j check full` 的导入失败收敛逻辑：当生成模块的 `module_runtime.py` 或 `ui/__init__.py` 本身存在缺失依赖/导入错误时，CLI 现在会输出明确的文件级校验错误并返回失败，不再直接抛出 traceback；新增回归测试已锁定这两条失败路径，`uv run pytest -q` 当前为 `337 passed`。
- 最新修复：2026-04-16 已压缩仪表盘页面上半部分的标题区、统计卡高度与纵向间距，并抬高“系统实时日志”区域的最小高度；默认窗口尺寸下首页可见日志行数明显增加，便于直接观察任务执行输出。
- 缺陷：BUG-003-pyqt-runtime-blocked-by-system-policy、BUG-004-zip-upgrade-leaves-stale-files、BUG-005-hybrid-acquisition-mode-declared-but-rejected、BUG-013-module-assembler-import-errors-hidden

## 下一步建议

- 检查任务人天估算是否真实合理，仅在必要时再细化到 0.5 人天精度
- 若进入设计或实施阶段，先确认 `docs/04-project-development/04-design/technical-selection.md` 已明确框架、模块、后台范围和编码规则
- 模块入口自动托管方案已闭环；当前应先完成 `TASK-023` 的 PR 收口与真实业务模块接入验证，再处理真实站点 E2E 与发布收口
- 执行 `ctrip` 真实站点 E2E 时，统一按 `docs/04-project-development/06-testing-verification/ctrip-real-site-e2e-closeout.md` 收口，不再用临时聊天清单替代正式步骤
- 优先处理外部 VirtualBrowser 运行时故障；当前 `localhost:9002` 上直接 `addBrowser -> launchBrowser` 也会稳定报 `Failed to detect DevTools port`，在外部应用恢复前不要把该阻塞继续归因到宿主 REM 代码
- 调试模块 UI 时，优先使用 DevLink 并在详情页通用数据表中点击“刷新”验证最新 `declare_ui` / handler 行为
- 若 UX/UI 需要可视化评审，优先登记真实设计交付物而不是只写文字
- 模块 UI 重构主链已闭环；当前优先做 hosted UI PR 收口与真实业务模块接入验证，不要重新引入 `micro_app` / `ui:*` 兼容层
- 若工作项进入收尾，确认关联 PR 已完成评审并合并
- 阶段切换前先更新正式文档，再刷新 `/.factory/memory/` 压缩记忆
- 最新修复：2026-04-22 已完成代码洁净性专项第一轮收口。REM 回收失败不再把环境误标为 `READY`；ATM 删除作业改为“先停调度/请求停机，再等 active task 归零后删库”；MMS 外部模块安装目录已改为稳定模块名并兼容旧非规范目录迁移/回滚；模块记录写入接口对坏输入改为 fail-fast；SDK `page create` 输出已对齐宿主 `module`/无参实例化契约，`module init/set version/release status` 同步校验 `pyproject.toml` 与 `module.yaml` 版本一致；开发者指南里的页面示例签名也已同步更新。
- 最新修复：2026-04-22 已完成代码洁净性专项第二轮收口。`db_cli reset` 改为同时清空 `config.db` / `state.db` / `data.db`；`module init` 生成的默认任务测试脚手架改为可直接运行；`page create --force` 现会同步刷新 manifest 与 runtime helper block，避免页面 helper 漂移；ATM `returns_none` 选择器提示与运行时校验统一收口到共享 helper；REM 删除了伪 GC loop 状态机与死的外部同步兼容壳；旧 `src/ui/core` 兼容层与 `ModuleDetailPanel` 已正式退出主线。全量质量门当前为 `uv run ruff check .` 与 `uv run pytest -q` 通过（`640 passed`）。
- 最新修复：2026-04-23 `CR-015 / TASK-029` 已继续收口到“共享纯 UI 表格 + 显式 provider schema”边界：当前正式宿主表格与 hosted page 内联表格统一使用 `src.ui.components.data_table.SkyDataTable`，SDK `hosted_ui.py` 不再接受内联 `DataTable` 顶层 `binding` / `rows` 兼容写法，页面 schema 现在必须显式声明 `data_source`，以避免旧 schema 再次渗透回新的共享组件。
- 最新修复：2026-04-23 宿主表格不再把 Qt 垂直表头误当成“序号列”。`SkyDataTable` 默认已隐藏系统行头，当前只有 `TaskListWidget` 与 `ModuleListWidget` 显式声明 `__index__` 序号列，并在 provider 侧按当前分页结果计算连续序号。
- 最新修复：2026-04-24 宿主入口已继续收口 Windows 打包态 `qasync` 退出竞态。`src.ui.app` 新增 `_ShutdownController`：在 `quitOnLastWindowClosed(False)` 基础上，同时监听 `lastWindowClosed`/`aboutToQuit`，并显式拦截 `QEvent.Type.Quit`，把真正退出延后到 `TaskService.stop()`、`DebugService.shutdown()` 与 `PlaywrightManager.force_shutdown()` 完成之后；对应 `test_app.py` 已补上 `Quit` 与 `lastWindowClosed` 两条定向回归，定向 `pytest` 与 `ruff` 通过。
- 最新修复：2026-04-24 共享危险确认框 `src.ui.components.confirm_dialog.ConfirmDialog` 已改为完整深色主题：标题、正文、取消按钮与危险按钮都改为稳定对象名和统一样式选择器，删除确认面板不再混入 macOS 系统默认黑字/浅色按钮；新增 `tests/unit/test_ui/test_confirm_dialog.py` 锁定背景色与按钮选择器，定向 `pytest` 与目标文件 `ruff` 已通过。
- 最新修复：2026-04-24 Hosted UI `Page` 现已支持页面级滚动配置 `scroll.vertical = auto|hidden`。宿主 `ManagedPageRenderer` 会按页面 schema 切换外层 `QScrollArea` 的竖向滚动条策略，默认仍是 `auto`，只有显式声明 `hidden` 的页面才会隐藏右侧滚动槽；新增 `test_hosted_ui_card.py` 与隔离桥接层的 `test_managed_page_scroll.py` 回归锁定该行为。另：为跑通这组 focused test，`ManagedPageRenderer` 已把 `RUNTIME_SURFACE_FULL` 改成局部懒加载，避免宿主页面 UI 测试在导入阶段过早触发 ATM/REM 环状依赖。
- 最新修复：2026-04-25 已修正 `state.db.environments` 外部环境导入唯一性：启动迁移会删除旧 provider 扩展列，并对同一 `(provider, name)` 只保留最早一条名称，其余重复记录仅清空 `name`、不删除环境记录；旧外部 ID 不再影响唯一索引。新增 `test_state_db_migration.py` 锁定旧库兼容路径。
- 最新修复：2026-04-25 已压缩仪表盘统计区并扩大系统实时日志区域。Dashboard 统计卡片使用 compact 版 `StatCard`，始终按 6 个卡片一行排列；日志控制台最小高度提升到 520px。新增/调整 `test_dashboard.py` 锁定单行布局与日志高度，`test_stat_card.py` 保持默认卡片行为不变。
- 最新修复：2026-04-25 已把宿主公共 UI 边界继续收口：新增 `open_dialog_async`、`ChoiceDialog`、`NoticePanel`，并为 `MessageDialog` / `ConfirmDialog` 补齐 async API；REM/MMS/ATM 受影响路径不再直接依赖 `QMessageBox` / `QDialogButtonBox`，创建/导入/编辑环境、IP 池弹窗、模块安装预览、模块列表提示/确认、托管页 CRUD 表单和手动批次中止选择均改走公共组件。新增 `test_public_ui_boundaries.py` 锁定 core UI 禁用原生消息框/按钮盒，相关目录回归 `448 passed`。
- 最新修复：2026-04-25 已新增公共 `ProgressDialog` 进度弹窗，并把 REM 环境列表、MMS 模块列表、ATM 任务列表里原本嵌在客户端面板内的进度条统一改为弹窗式进度反馈，完成后自动关闭；公共消息/确认/选择/进度弹窗全部显式保留原生标题栏。新增 `test_progress_dialog.py` 与 `test_public_ui_boundaries.py` 边界断言，锁定 core UI 不再直接使用 `QProgressBar` 或无标题栏弹窗；全量回归 `uv run pytest -q` 为 `731 passed`。
- 最新修复：2026-04-25 已针对真实 UI 截图暴露的三个问题继续收口：新增公共 `configure_titled_dialog`，所有 core QDialog 与托管页动态 CRUD 表单统一强制使用原生标题栏，创建环境/已有环境导入不再出现无标题面板；VirtualBrowser 销毁会等待窗口关闭和外部删除结果稳定后再判定失败，并把失败原因透传到 REM UI；ATM 手动批次启动进度弹窗改为非 modal，Dispatcher 在任务进入 `RUNNING` 时发布 `TASK_STARTED`，任务列表订阅后可及时关闭“环境启动中”进度弹窗并保留中止操作可用。全量回归 `uv run pytest -q` 为 `738 passed`。
- 最新修复：2026-04-26 已修复 REM 环境列表删除时把表格字符串 ID 传入 `EnvironmentManager.destroy_env()` 后误判“环境不存在”的问题；`EnvPool` 现在统一把数字字符串规范化为整数键，覆盖 `get/remove/update_status/env_metadata` 入口，环境销毁、启动、停止、编辑与元数据查询不再因 `"282"` 和 `282` 类型不一致而失效。新增回归 `test_destroy_env_accepts_numeric_string_id_from_ui`，REM 目录回归 `119 passed`，全量回归 `739 passed`。
- 最新修复：2026-04-26 已把“从已有环境导入并关联任务”的启动进度对齐任务列表执行路径：`ExecutionRunner` 在复用环境 `start_env()` 完成前保持任务为 `PENDING` 并显示 `环境启动中`，REM 环境列表导入流程会等待关联任务离开 `PENDING` 后再关闭公共进度弹窗，避免导入后到浏览器窗口打开之间出现无反馈空档。定向回归覆盖执行器状态更新与环境导入进度等待。
- 最新修复：2026-04-27 已完成一轮仓内公共 UI 收口。新增 `StyledCheckBox`、`StyledTextEdit` / `StyledPlainTextEdit`、`SegmentedOptionControl`、公共 `ToggleSwitch`，并扩展 `StyledButton` 到 `ghost` / `text` 变体与运行时 variant 切换。`run_profile_dialog.py`、`task_debug_dialog.py`、`task_detail_dialog.py`、`task_confirmation_dialog.py`、`config_center_page.py`、`module_detail_page.py`、`module_install_dialog.py`、`module_list_widget.py`、`about_dialog.py`、`help_page.py`、`dashboard.py`、`ip_pool_dialogs.py` 已统一到公共组件库；`test_public_ui_boundaries.py` 现用 AST 锁定这些页面不得再直接从 Qt 导入 `QPushButton` / `QCheckBox` / `QComboBox` / `QLineEdit` / `QPlainTextEdit` / `QSpinBox` / `QTextEdit`。全量质量门当前为 `uv run ruff check .`、`git diff --check`、`QT_QPA_PLATFORM=offscreen uv run pytest -q -p no:cacheprovider` 通过（`798 passed`）。
- 最新修复：2026-04-27 已纠正任务监控页“双进度”回归。根因是 `TaskListWidget` 一边使用 Shell 级 `TaskProgressPresenter -> ProgressDialog`，一边仍沿用 `SkyDataTable.set_loading()` 的内联 loading bar。现在 `SkyDataTable` 新增声明式 loading 策略，任务监控 schema 已改为 `loading.inline = false`、`disable_interaction = false`，该页只保留公共进度弹窗，不再叠加表格内进度条。定向回归 `test_data_table.py`、`test_task_list_widget.py`、`test_public_ui_boundaries.py` 通过（`28 passed`）。
