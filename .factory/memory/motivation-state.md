# 动能状态

- 更新时间：2026-03-27
- 当前阶段：IMPLEMENTATION
- 当前模式：Default
- 当前主目标：保持工厂基线与正式文档树一致，避免再次出现入口、版本、文档索引和运行事实漂移

## 当前推进重点

1. 保持 `docs/`、`.factory/memory/` 和代码事实同步。
2. 继续把剩余工作聚焦在真实站点 E2E 与发布收口，不重新打开已关闭的编号任务。
3. 新发现的问题优先判断是否属于模式级缺陷，再决定是记为 `BUG`、`CR` 还是发布阻塞。

## 当前正向信号

- 默认质量门当前可复验：`uv run pytest -q`、`uv run ruff check .`、UI smoke、build 均已通过。
- `docs/` 已收敛为单一 Markdown 文档树，并补齐目录入口。
- MMS 合规收口与 SDK CLI 初始化向导均已完成。

## 当前风险与注意项

- `ctrip` 真实站点 E2E 仍未完成，是当前最主要剩余风险。
- 工作区可能保持脏状态；处理单一任务时不要顺手清理无关改动。
- `docs/.obsidian/` 不是正式文档树的一部分，不纳入目录治理和提交范围，除非用户明确要求。

## 主动性规则

- 允许主动补齐同类缺口，但不要无边界扩展范围。
- 如果发现规则文件缺失、索引失真或验证口径漂移，优先修基线，再继续实现。
- 如果遇到外部依赖、浏览器、凭据或真实环境约束，先记录事实，再请求必要授权，不做隐式假设。
