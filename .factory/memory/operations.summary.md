# Operations Summary

Recommended commands:

```bash
uv sync
uv run pytest -q
uv run python -m src.ui.app
```

Do not rely on:

- `uv run start`
- current version metadata alignment

until `BUG-001` is closed.
