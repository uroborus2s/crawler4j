# TASK-007 删除内部组件并切换到外部安装模块模式

- 状态：DONE
- 类型：TASK
- 优先级：P0
- 估算：2.0 人/天
- 关联 ID：`TASK-007`, `BUG-002`, `REQ-002`

## 目标

- 清理内置组件实现
- 将模块运行切换到外部安装模块模式
- 重点对接 `/Users/uroborus/PythonProject/ctrip_crawler`
- 实施时先吸收 `BUG-002` / `TASK-003` 中与 `ctrip` 当前运行时恢复直接相关的内容

## 验收标准

- Core 可以通过外部安装包发现并加载目标模块
- 仓库内不再保留重复的内部组件实现
- 安装与运行链路在外部模式下可验证
- 不把当前已知失效的 `labor_workflow` 运行时问题原样迁移到外部仓库

## 完成情况

- 仓库内置业务模块已删除，仅保留 `modules/README.md` 作为占位说明
- Core 已切换为外部安装模块主链路，并验证了 zip 安装后的发现与加载
- 安装正式模块时会自动移除同名 `dev link`，避免运行时误回落到开发源码目录
- `BUG-002` 已在后续专项修复中关闭：已恢复旧 `src.automation.*` 兼容路径，打包后的 `ctrip` 外部模块不再因缺少导入而退化为登录流程
