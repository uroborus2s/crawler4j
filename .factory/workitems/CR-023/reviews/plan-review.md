## 计划评审

**状态：** 通过

**输入：** 用户已确认根因、首选依赖修复、发布物要求、禁止降级规则和三类环境验收口径；计划逐项映射到 AC-023-001 至 AC-023-007。

**问题：**

- 无阻塞问题。

**建议：**

- Windows 冻结物必须由 Windows 构建机复验，当前 macOS 证据不得替代。
- 通用模块依赖/能力协商保持后续架构项，不并入本次修复。

## 架构修订评审

**状态：** 通过

**批准证据：** 用户明确要求“按照宿主统一提供方法的方式，不能让模块自己去安装库”。修订方案复用现有 `TaskContext.tools` 扩展面，新增 `API-024 http.request`，第三方网络栈保持 Core 私有实现。

**边界：** crawler4j 交付宿主方法与发布依赖；`ctrip_crawler` 外部仓库必须另行改接该方法，完成前不得声明业务 E2E 已通过。
