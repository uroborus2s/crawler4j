# Review Response

Verified. “限制在当前池”不能解释当前池内条目缺失，原结论已撤回。

Evidence:

- 同池两个 `available`、未过期条目可以同时显示：聚焦测试 `1 passed in 0.49s`。
- 弹窗会静默过滤 `disabled` 或已过期条目。
- IP 池页面的状态列不合并过期状态，导致过期条目可能显示“可用”却无法进入下拉框。
- 截图实例环境 185 不在当前本机数据库，尚需该实例的条目状态与过期时间才能确认具体命中分支。
