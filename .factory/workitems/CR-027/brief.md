# CR-027 VirtualBrowser 创建使用浏览器自动匹配

- 状态：`ready_for_commit`
- 类型：CR
- 来源：用户明确要求

## 目标

用户 2026-09-06 最新要求取代此前固定上海时区的要求：新建环境参考正常样本 139、141、142，语言、时区、定位跟随 IP，屏幕跟随电脑，UA 使用浏览器默认；仅取消固定语音列表覆盖；按用户本轮进一步明确，CPU/内存继续使用原常用硬件组合池。

## 验收

- 新建流程（包括携带旧 IP 表资料的分支）最终使用语言/时区/定位 mode 2、screen mode 0、ua mode 0，不把 IP 表语言/时区/坐标写成自定义指纹。
- 保留原 CPU/内存组合池（4/8、4/16、8/16、8/32、12/32）及池外组合纠正/验收。仅语言、时区、定位、屏幕、UA、Speech Voices 按三个样本对齐；speech_voices 使用 {mode:1,value:{}}。不强制 WOW64 转换或固定语音列表，不复制样本 Cookie/设备身份。
- 创建与运行期校验接受这些模式，保留主版本一致性、结构合法性、代理配置一致性和适用的显式值校验。
- 复用相同随机指纹流程的显式刷新入口保持规则一致；不实际刷新或迁移已有环境。
- 对新增语义先获得失败测试，再运行相关 VirtualBrowser 单元回归、Ruff 与代码形状检查。
- 此次是用户指定的配置行为变更，不声称已经修复目标网站 203。

## 范围

- 不改代理协议、代理 URL/回环转发策略、代理绑定或数据库逻辑；不连接代理或网站、不创建真实浏览器环境。
- 不改其他浏览器 Provider。

最新范围修正：本轮此前删除硬件组合限制的实现与审阅结论已被用户明确取代，需恢复组合限制后重新验证与复审。
