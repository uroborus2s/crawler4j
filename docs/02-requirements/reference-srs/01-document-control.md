# 第 1 章 文档控制信息（Document Control）

## 1.1 文档目的与适用范围

### 1.1.1 文档目的

本《系统需求与功能规格说明书》（SRS/FSD）用于对 **蛛行演略（crawler4j）** 平台进行一致、可审计的需求与规格定义，作为项目 **开发、测试、发布与维护** 的权威参考依据。

其主要作用包括（但不限于）：

- **统一口径**：对三层架构（Framework Core / SDK / Modules）给出统一职责边界、接口契约、数据与状态模型。
- **指导实现**：为研发提供可落地的功能拆解、接口定义、数据流与错误处理要求，降低实现偏差。
- **指导测试**：为 QA/QE 提供可追溯的验收标准、用例设计依据与失败注入点。
- **支持运维**：为运行监控、排障与升级迁移提供约束、运行手册要点与兼容性规则。

### 1.1.2 适用范围（Scope）

本规格说明书覆盖蛛行演略（crawler4j）在“微内核 + SDK + 插件/模块”的体系下，包含：

- **项目级**：目标/非目标、边界、关键概念、总体架构与横切关注点。
- **系统级**：Framework Core / SDK / Modules 三大子系统的自包含规格。
- **模块/功能级**：对关键能力（例如模块加载、任务执行、环境回收、UI Host 微前端承载）给出可实现、可测试、可追溯的规格。

### 1.1.3 不在范围内（Out of Scope）

以下内容不作为本规格说明书的强制覆盖范围（如需可在后续专项文档补充）：

- 具体站点的页面结构、选择器细节与反爬策略细节（属于各业务模块的实现文档/代码注释范畴）。
- 第三方服务本身的 SLA/合规条款（如指纹浏览器/接码平台的服务条款）。
- 与业务无关的组织流程制度（如人事审批、采购流程等）。

## 1.2 读者对象与使用方式

### 1.2.1 目标读者

- **架构/核心研发（Core Maintainers）**：关注资源治理、调度与隔离、GUI 异步模型、全局可靠性。
- **SDK 维护者（SDK Maintainers）**：关注 API 契约、兼容性策略、CLI 与脚手架质量。
- **业务模块开发者（Module Developers）**：关注 Modules 规范、TaskScript/TaskFlow 模板、错误处理与可恢复执行。
- **测试/质量（QA/QE）**：关注验收标准、测试矩阵、失败注入、回归策略与可追溯性。
- **运维/值守（Ops/SRE）**：关注运行依赖、发布方式、日志与审计、排障路径、升级与迁移。
- **安全审计/合规（Security/Compliance）**：关注沙箱与隔离边界、危险能力限制、敏感数据脱敏与审计。

### 1.2.2 阅读指南（快速定位）

- 想理解系统全貌：优先阅读 **第 4 章 项目架构设计**。
- 负责 Core：阅读 **第 5 章 Framework Core**。
- 负责 SDK：阅读 **第 6 章 SDK**。
- 负责 Modules：阅读 **第 7 章 Modules** 与对应业务模块章节。
- 负责测试：以各子系统的“测试与验收”章节为主，并回溯到其“功能规格/数据契约”。

### 1.2.3 使用方式（作为“权威参考”的规则，MUST）

- 本文档与代码同仓库管理，以 Git 版本控制为准；任何影响对外契约/行为的变更必须同步更新本文档。
- 当本文档与实现不一致时：
  - 若实现为既定事实但未记录：必须补齐规格说明并标注变更原因。
  - 若实现偏离规格：必须修复实现或调整规格，并在变更记录中说明。

## 1.3 版本规则与变更流程

### 1.3.1 版本编号规则

本文档版本遵循 SemVer 思想：`MAJOR.MINOR.PATCH`。

- **MAJOR**：破坏性变更（Breaking Change），例如 SDK API/模块规范/数据契约不兼容。
- **MINOR**：新增向后兼容能力/规格（新增扩展点、可选字段、新流程）。
- **PATCH**：不改变外部行为的修订（措辞澄清、示例完善、错别字修复）。

### 1.3.2 变更流程与审阅机制

- 所有变更通过 Pull Request（PR）进入主分支；PR 描述必须注明：变更动机、影响范围、是否 Breaking。
- 变更需要至少相关角色之一审阅通过（Core/SDK/Modules/QA 视影响范围而定）。
- 若变更涉及外部接口/数据契约：必须补齐“兼容性说明”与“迁移建议”（见附录 F）。

### 1.3.3 版本历史（Change Log）

| 文档版本 | 日期       | 变更类型 | 变更摘要                                          | 影响范围                        |
| -------- | ---------- | -------- | ------------------------------------------------- | ------------------------------- |
| 1.0.0    | 2026-01-10 | RELEASE  | Initial Release (Refactored from requirements.md) | 全局 (Breaking Change: Doc Structure) |

## 1.4 术语规范与标记约定

### 1.4.1 规范性关键词（MUST/SHOULD/MAY）

本文档中的规范性关键词采用 RFC 2119 / RFC 8174 的定义（当且仅当使用大写形式时具备规范性约束）。

- **MUST / SHALL / REQUIRED**：必须满足；不满足即视为不符合规格。
- **MUST NOT / SHALL NOT**：绝对禁止；出现即视为违反规格。
- **SHOULD / RECOMMENDED**：强烈建议；允许例外，但必须给出理由并记录。
- **MAY / OPTIONAL**：可选；若支持则必须按本文档约束实现。

### 1.4.2 附加标记约定

- **[GAP]**：理想规格与当前实现存在差距，需要补齐实现或调整设计。
- **[RISK]**：已知风险点（性能/稳定性/合规/安全），需要在测试或运维中重点关注。
- **[TODO]**：明确的后续工作项（不作为“待补充内容”的占位符使用）。

### 1.4.3 术语一致性要求（MUST）

- 关于关键对象（Environment/TaskScript/TaskFlow/TaskContext/Module/Plugin/Hook）的命名必须与代码与 UI 展示保持一致。
- 当同一概念存在别名时，必须在首次出现处给出“首选术语 + 别名”。

## 1.5 编号与追溯规范（MUST）

### 1.5.1 需求编号

- 用例：`UC-xx`
- 功能需求：`FR-{SYS}-{DOMAIN}-{NNN}`
  - `SYS`：`CORE | SDK | MOD`
  - `DOMAIN`：建议与能力域一致（例如 Core：`MM/REM/TSM/ATM/UI/X`）
- 非功能需求：`NFR-{SYS}-{QUALITY}-{NNN}`

### 1.5.2 追溯规则

- 每个 FR/NFR 必须在附录 D 中能追溯到：章节位置、关键设计点、测试用例（或验收口径）

## 1.6 写作模板入口

写作模板见：`srs/templates/`。

## 1.7 参考资料索引

### 1.7.1 仓库内参考资料（Repo References）

- `README.md`：项目总体介绍与快速入门
- `crawler4j_sdk/`：SDK 源码（TaskScript/TaskContext/TaskResult/CLI 等）
- `src/`：Core 源码（调度、环境管理、插件系统、GUI）
- `modules/`：内置业务模块示例
- `migrations/`：数据库迁移脚本与版本演进
- `pyproject.toml` / `uv.lock`：依赖与构建配置（以 uv 为准）

### 1.7.2 外部技术文档与标准（External References）

- RFC 2119 / RFC 8174
- Semantic Versioning 2.0.0（SemVer）
- Python / uv / Playwright / PyQt6 / qasync / SQLite 官方文档

## 1.8 递归展开模板（章节编制要求，MUST）

- **系统级/模块级**：必须写明职责边界、接口与数据契约、关键流程与异常、验收标准。
- **功能级**：必须至少包含：功能说明、流程图、数据/存储设计、类/接口设计、交互设计（若对 UI 可见）。
