# 04 模块开发概览

本目录收纳 TaskScript、Workflow、CLI/UI 配置和 Core 能力说明。
这里的所有数据示例都以 `ctx.db` 最小能力接口为准，旧 `DataService` 口径不再适用。

建议阅读顺序：

1. 4.1 编写 TaskScript
2. 4.2 编写 Workflow
3. 4.3 CLI 命令与 UI 配置
4. 4.4 Core 提供的能力清单
